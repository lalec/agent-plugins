---
name: upgrade
description: Upgrade an existing dev-workflow install in the current project. Detects gaps against the latest templates (agents, hooks, lifecycle skills, commands, deploy contract) and applies idempotent fixes after user confirmation. Claude Code only. Trigger when the user wants to upgrade, update, sync, or refresh an existing dev-workflow install.
---

# upgrade

> **Hard gate.** If no `.claude/agents/<PREFIX>-dev.md` exists for any prefix, abort and tell the user: "No existing dev-workflow install detected. Run `/dev-workflow:install` instead." Do **not** continue.
>
> **You MUST execute steps 1–8 in order. Do not summarize the existing install or claim it is "complete" without running step 5 (the diff checklist) — every running install has gaps unless every checklist item from step 5 has been verified absent. Skipping step 5 is a workflow violation.**

Upgrades an existing dev-workflow install to the current templates. Idempotent — safe to re-run. Reads templates from `../../shared/`, captures existing state from `.claude/`, presents a gap diff, applies confirmed fixes, verifies.

---

## Preflight

Read `../../shared/preflight.md` and follow it before continuing.

---

## Step 1 — Capture existing state

Read current files (do not prompt the user for things already known):
- `EXISTING_PREFIX` — read from `.claude/hooks/governed-paths.conf` `PATH_MAP` entries or from agent filenames (`<PREFIX>-dev.md`)
- `EXISTING_SKILLS[]` — list `.claude/skills/`

`CONFIG_DIR=.claude` and `PROJECT_FILE=CLAUDE.md` are fixed (Claude Code only).

---

## Step 2 — Refresh category map

Run a fresh Phase 1c (category discovery) — read `../../shared/tpl-domain-skill.md` § Domain categories. Use existing `EXISTING_SKILLS` as the skill names — do not propose renames. If category discovery surfaces categories not currently owned by any existing skill (e.g. project added Observability since first install), warn the user and recommend invoking `<PREFIX>-skill` after the upgrade to register the new ownership; do **not** create new skills as part of the upgrade.

---

## Step 3 — Scaffold missing foundational components

No confirmation needed — these are absent, not stale.

Read `../../shared/tpl-agents.md` and `../../shared/tpl-commands.md` now.

- **Missing commands** — if `.claude/commands/` is absent or any of `code.md`, `fix.md`, `pilot.md`, `tweak.md`, `revert.md`, `tidy.md`, `roadmap.md`, `wrap.md` are missing: create `.claude/commands/` if needed, then create each missing command file from the corresponding template in `tpl-commands.md`, substituting `<PREFIX>` and `<PROJECT>`. Announce what was created.
- **Missing docs stubs** — create `docs/roadmap.md` and `docs/project-log.md` if absent (same stubs as install Phase 2).
- **Design command** — if a frontend/design domain skill is detected and `.claude/commands/design.md` is missing: create it from `tpl-commands.md § /design`.

Do **not** create or restructure skill files in this step — skills are owned by `<PREFIX>-skill` after install.

---

## Step 4 — Detect LEGACY_DEPLOY_OWNER

Used only by the migration to lifecycle deploy below. Scan `EXISTING_SKILLS` for one with both a `## Deployment` section in its SKILL.md AND a `references/deploy-config.yaml`. If found and that skill is not `<PREFIX>-deploy`, capture its name as `LEGACY_DEPLOY_OWNER`. Otherwise leave empty.

---

## Step 5 — Diff against the gaps the upgrade fixes

Present to the user as a checklist before changing anything:

- [ ] `<PREFIX>-deploy` lifecycle skill is missing — needs creation from `tpl-lifecycle.md § tosk-deploy/SKILL.md`
- [ ] `deploy-config.yaml` lives in `<LEGACY_DEPLOY_OWNER>/references/` instead of `<PREFIX>-deploy/references/` — needs migration
- [ ] A domain skill (`<LEGACY_DEPLOY_OWNER>` or any other) carries a `## Deployment` section — deploy logic now lives only in `<PREFIX>-deploy`
- [ ] `governed-paths.conf` `PATH_MAP` maps deploy-mechanism paths to `<LEGACY_DEPLOY_OWNER>` instead of `<PREFIX>-deploy`
- [ ] `governed-paths.conf` missing `DEPLOY_PATHS` variable
- [ ] `ref-sync-check.sh` hardcodes path patterns inline (grep for `grep -qE` with literal directory names) instead of sourcing `governed-paths.conf`
- [ ] `<PREFIX>-dev.md` step 4 contains inline yaml-handling rules (`verify: local`/`cloud:<env>`) — superseded by the one-line delegation in `tpl-agents.md § tosk-dev` step 4
- [ ] `<PREFIX>-dev.md` step 4 references `<PREFIX>-deploy` as a domain skill — wording should treat it as a lifecycle skill
- [ ] `<PREFIX>-dev.md` step 6 contains the legacy `invoke the <PREFIX>-qa agent directly` wording (causes subagent context confusion)
- [ ] `<PREFIX>-qa.md` step 7 contains the legacy `invoke the <PREFIX>-pm agent directly` wording (causes subagent context confusion)
- [ ] `<PREFIX>-dev.md` is missing an explicit `Commit` step between Reference Sync and Hand off
- [ ] `<PREFIX>-qa.md` is missing an explicit `Reference Sync` step between Sign off and Hand off
- [ ] `<PREFIX>-pm.md` is missing an explicit `Reference Sync` step between Docs check and Verify log written
- [ ] `.claude/commands/log.md` exists — superseded by `/wrap`, which covers the same use case with broader framing
- [ ] `<PREFIX>-log/SKILL.md` entry format missing the `Deployed:` field (optional touch — ask user)
- [ ] `<PREFIX>-design` exists but the skill that owns the Frontend category is missing the `## Visual Decisions — Delegate to <PREFIX>-design` section
- [ ] `<PREFIX>-design/SKILL.md` description is permissive ("Use when...") rather than mandatory ("MUST be invoked before...")
- [ ] `<PREFIX>-review/references/verification-before-completion.md` exists — superseded by `<PREFIX>-debug/references/verification.md`
- [ ] `<PREFIX>-qa.md` has separate `Determine test tier` (step 3) and `Test` (step 4) steps with a `Sign-off criteria` tier table — superseded by collapsed step 3
- [ ] Agents (`<PREFIX>-dev`, `<PREFIX>-qa`, `<PREFIX>-pm`) have unconditional Reference Sync steps — superseded by the gated form
- [ ] Agents carry a `## What this agent does NOT do` section — superseded by the one-line `## Boundaries` paragraph
- [ ] `<PREFIX>-debug/SKILL.md` carries the legacy verbose body — superseded by the trimmed `## Read Map` + `## References` form. (A deliberate customization is not a gap: a body tailored to the project, or project-authored references with genuine sync needs, stays — with `## Reference Sync` scoped to exactly the project-authored files, per Global Invariant 5's ownership rule.)
- [ ] `<PREFIX>-review/SKILL.md` inlines protocol detail AND a `## Reference Sync` section — superseded by the trimmed `## Read Map` + `## References` form (no Reference Sync). (Same customization carve-out as `<PREFIX>-debug` above.)
- [ ] `<PREFIX>-skill/SKILL.md` parity invariant is the blanket form ("every skill must have a Reference Sync checklist") without the static-content exception and the content-ownership rule — superseded by Global Invariant 5 in `tpl-lifecycle.md § tosk-skill` (static-template references exempt; project-authored references always synced, wherever they live)
- [ ] `<PREFIX>-deploy/SKILL.md` `## Deployment` section is missing the **Caller contract** (target=non-prod / target=prod), **Fill-in pass**, or inline-gate **context block** (env / url / command / trigger / commit / branch)
- [ ] `<PREFIX>-deploy/SKILL.md` frontmatter **contains** `disable-model-invocation` — must be removed; the gate blocks the `<PREFIX>-dev` deploy step (`target=non-prod`) and `/code|/fix --prod` (`target=prod`) Skill-tool calls the workflow depends on. Deploy must be model-invokable; "never on whim" comes from the description + the prod `user_confirm` gate
- [ ] `<PREFIX>-dev.md` step 4 does not pass `target=non-prod`
- [ ] `<PREFIX>-dev.md` step 4 still contains the trailing "If a gate is declined or skipped" clause — unreachable once gates are inline
- [ ] `<PREFIX>-pm.md` **has** a prod-deploy step (step 1.5 / `target=prod`) — superseded by the top-level `/code|/fix --prod` step (the deploy gate's `AskUserQuestion` can't reach the user from a subagent); must be removed
- [ ] `<PREFIX>-pm.md` `## Boundaries` lacks the deploy-tangent guard ("Do not read `deploy-config.yaml`…") — without it pm can follow a stray deploy instinct, burn its turn on deploy reasoning, and never write the log
- [ ] `<PREFIX>-pm.md` lacks a terminal `## Response Requirements` / `## Handoff` block — pm is the only agent without one, so its outcome isn't machine-parseable and it tends to echo `<PREFIX>-qa`'s `signed-off` instead of emitting its own `Status: complete | blocked`
- [ ] `<PREFIX>-pm.md` frontmatter lacks `model: sonnet` — pm's work is mechanical close-out (verify evidence → log → docs), so Opus is overkill (~4× the cost); dev/qa stay on Opus
- [ ] `<PREFIX>-dev.md` step 7 (Hand off) does not require a structured `## Handoff` block
- [ ] `<PREFIX>-qa.md` step 2 (Address findings) does not contain "You do not edit code"
- [ ] `<PREFIX>-qa.md` step 6 (Hand off) does not require a structured `## Handoff` block
- [ ] `<PREFIX>-pm.md` step 1 (Verify QA phases) says "scan the session transcript" without specifying the jsonl grep pattern
- [ ] `<PREFIX>-pm.md` step 1 contains the literal `PROJECT_ENCODED=` jsonl-grep pattern — superseded by reading `**QA-evidence:**` from the invocation prompt
- [ ] `.claude/commands/code.md` / `fix.md` lack a `--prod` flag parse (Step 0) and the final top-level prod-deploy step that invokes `<PREFIX>-deploy target=prod` after pm
- [ ] `<PREFIX>-qa.md` is missing the `## Invocation modes` section (`mode: initial` / `mode: retest`)
- [ ] `.claude/commands/code.md` Step 2 does not parse the dev handoff block, branch on qa Status, or re-spawn with `mode=retest` after a dev fix
- [ ] `.claude/commands/code.md` Step 3 does not pass `**QA-evidence:**` to pm
- [ ] `.claude/commands/fix.md` Step 3 does not parse the dev handoff block, branch on qa Status, or re-spawn with `mode=retest` after a dev fix
- [ ] `.claude/commands/fix.md` Step 4 does not pass `**QA-evidence:**` to pm
- [ ] `<PREFIX>-dev.md` carries the `## Handoff` block format inside step 7 of `## Workflow` and has no terminal `## Response Requirements` section — the strengthened wording lives mid-file (recency loss), and agents still drop the block. Superseded by the structural form in `tpl-agents.md § tosk-dev`: step 7 is a one-line pointer; the block format + imperative live in a final `## Response Requirements` section.
- [ ] `<PREFIX>-qa.md` carries the `## Handoff` block format inside step 6 of `## Workflow` and has no terminal `## Response Requirements` section — same drift as dev. Superseded by the structural form in `tpl-agents.md § tosk-qa`.
- [ ] Domain skills are missing the `## Preconditions — Verify Before Writing References` section between `## Architecture` and the next section — this is the rule that requires grepping for the definition of any referenced identifier before emitting the reference (closes the "wrote `var(--undefined-token)`" failure class)
- [ ] `<PREFIX>-review/references/issuing-findings.md` is missing, or `<PREFIX>-review/SKILL.md` `## Read Map` lacks the `Issuing review findings` branch — the rule that blocking findings need a file:line citation read in this session (closes the "false-positive blocking finding" failure class)
- [ ] `<PREFIX>-review/references/security-review.md` is missing, or `<PREFIX>-review/SKILL.md` `## Read Map`/`## References` lack the `security-review` entry — the fast per-task agent-miss security pass (deps/supply-chain, secrets, injection sinks, access-control/IDOR, insecure defaults + headers, dead surface) with recon→verify triage and escalation to the built-in `/security-review`
- [ ] `<PREFIX>-test/references/custom-tests.yaml` is missing — per-task functional-feature store captured by `/code` and `/fix` (needs scaffolding)
- [ ] `<PREFIX>-test/references/custom-tests.md` is missing — schema + runtime execution/inference protocol + prior-selection rule (needs scaffolding)
- [ ] `<PREFIX>-test/references/test-commands.md` lacks `## Smoke` / `## Regression` / `## Functional Feature Subjects` headings — needs restructure, preserving existing content
- [ ] `<PREFIX>-test/SKILL.md` still has a `## E2E Browser Tests` section or a `<FRONTEND_PATHS>`→E2E row in `## Test Plan` — superseded by the 3-tier form (Smoke / Functional Feature / Regression) + Rule-5 pointer to `custom-tests.md`. (A per-verification `type: E2E` **value** in `custom-tests.*` is the current design — do **not** remove it; this gap is only the legacy section/row.)
- [ ] `<PREFIX>-test/references/sync-checklist.md` contains the "E2E browser target URL" line — superseded by the `custom-tests.yaml` assert/type/paths trigger
- [ ] `<PREFIX>-qa.md` `## Invocation modes` lacks `regression_mode`, or step 3 does not forward `regression_mode` / new-entry names / `Files changed:` paths to `<PREFIX>-test`
- [ ] `.claude/commands/code.md` / `fix.md` lack Step 0.5 (capture) and the persist step, **or** their persist step writes `task`/`assert` unquoted (invalid-YAML risk) / includes `.claude/**` in `paths`, **or** the Step 0.5 capture is a prose prompt instead of the AskUserQuestion that proposes 2–3 candidate verifications — superseded by the current `tpl-commands.md` (single-quoted scalars, behavioral-surface `paths`, candidate-proposal capture, "verification" vocabulary)
- [ ] **`paths` reduction is the flat `.claude/**` exclusion** rather than the behavioral-surface rule — grep `code.md`/`fix.md` for `behavioral surface`. The flat form is wrong in both directions: it lets documentation-only paths (`docs/**`, `*.md` outside source trees, any docs-only tree the project keeps) drive prior-selection even though they cannot break an assertion, and it drops executable source that happens to live under `.claude/` (e.g. `.claude/skills/**/scripts/**`), which genuinely is behavioral surface. Needs the current rule from `tpl-commands.md § /code` Step 1.5, including the "if the reduction leaves `paths` empty the entry is mis-anchored" check
- [ ] `<PREFIX>-test/references/custom-tests.md` is missing **or** lacks the `Running-stack rule` (in-process test runners like FastAPI `TestClient` must report blocked, not a green pass) / single-quoted schema — needs create-or-re-sync
- [ ] `<PREFIX>-test/references/custom-tests.md` schema or `custom-tests.yaml` entries still use `surface: ui|api|data` — superseded by `type: UX|Integration|E2E` (`api`+`data`→`Integration`, `ui`→`UX`/`E2E`); needs re-sync + entry migration
- [ ] `deploy-config.yaml` uses the legacy split schema — a top-level `local:` block per component or a `local |cloud:` prefix in `verify:` — superseded by the unified env schema (every env declares exactly one of `run:`/`deploy:`; `verify:` is a plain env name); needs migration (`local:` → `envs.local`, `verify: cloud:<env>` → `verify: <env>`)
- [ ] a locally-runnable component in `deploy-config.yaml` has **no non-prod env** (no `envs.local` serve-env, no cloud non-prod ship-env) — typed UX/E2E verifications then have no target and report blocked (or, historically, got silently downgraded to component tests); needs an `envs.local` fill
- [ ] `<PREFIX>-test/references/custom-tests.md` Execution resolves UX/E2E to a bare "component url" (no first-non-prod-env rule, prod not excluded) **or** its Running-stack rule lacks the component/unit-test ban ("never count component/unit tests…") — superseded by the current `tosk-test` template
- [ ] `<PREFIX>-qa.md` step 5 (Sign off) lacks the blocked-verification mapping (a blocked verification → `Status: blocked`; no substitute tests; UAT-deferral is the command's call)
- [ ] `.claude/commands/code.md` / `fix.md` lack the "Ensure verification stack" pre-QA step (start a serve-env at top level when a UX/E2E target is down) or the UAT-defer branch in the qa-handoff parse
- [ ] `<PREFIX>-qa.md` handoff lacks the `signed-off-with-deferrals` status, the `Evidence:` field, or the `UAT-deferred:` field — superseded by the three-status vocabulary in `tpl-agents.md § tosk-qa` (kills the blocked→re-issue→pm-bounce churn and the status-laundering it caused)
- [ ] `<PREFIX>-qa.md` step 2 forbids all edits — superseded by the class split (non-source findings fixed directly + committed by name; source findings still always route to dev)
- [ ] `<PREFIX>-qa.md` step 1 lacks the per-file diff-read rule (no persisted full-diff files — they truncate and force re-reads)
- [ ] `<PREFIX>-pm.md` step 1 rejects `signed-off-with-deferrals`, or step 2 lacks the feature-commit hash rule + `UAT-deferred:` carry, or step 2.5 roadmap advancement is best-effort instead of required
- [ ] `<PREFIX>-dev.md` step 3 lacks the non-interactive rule (`CI=1` / `--yes` / timeouts — an interactive prompt killed a dev agent mid-pipeline)
- [ ] `.claude/commands/code.md` / `fix.md` lack any of: the "Gate policy" section (risk-split timeouts, never claim consent), `--no-push` flag, entry hygiene + plan shortcut in Step 0, the UAT-only intent rule in Step 0.5, the salvage protocol + no-top-level-edits rule, `stack:`-aware + freshness-aware ensure-stack, the `signed-off-with-deferrals` branch, `Feature commit:` + `UAT-deferred:` in the pm prompt, the "Close out: push + verified scorecard" step, or the evidence-first Done section
- [ ] `.claude/commands/tweak.md` missing — the sanctioned iterate lane (top-level, inline-verified, batched close-out)
- [ ] `.claude/commands/revert.md` missing — the sanctioned rollback (`git revert`, scoped re-verify, logged reversal)
- [ ] `.claude/commands/tidy.md` missing — the WIP-resolution lane; without it a dirty tree at `/code`/`/fix`/`/pilot` entry has nowhere to go but a blind stash. Also flag when `code.md` / `fix.md` / `pilot.md` entry hygiene still says "ask the user how to proceed" on overlapping WIP instead of routing to `/tidy`, or when the `CLAUDE.md` `## Agents` routing paragraph lacks the `/tidy` sentence
- [ ] `.claude/commands/wrap.md` lacks the conditional Step 0 review (ad-hoc source changes otherwise ship on self-review only) or the Step 4 push + scorecard (wrapped work otherwise accumulates unpushed commits)
- [ ] `<PREFIX>-deploy/SKILL.md` lacks the `## Push policy` section, the `gate: unanswered — parked` timeout return, or the non-prod early exit
- [ ] `<PREFIX>-test/references/custom-tests.md` lacks the **Freshness rule** (stale target = non-evidence) or the **Evidence trace** rule (per-verification command → observed → verdict lines feeding qa's `Evidence:` field)
- [ ] `<PREFIX>-log/SKILL.md` Process picks `git log -1` blindly instead of the primary feature commit, or the entry format lacks the `UAT-deferred:` line
- [ ] `pre-handoff-check.sh` matches only `tool_input.skill` — dead code in the pipeline, where qa is spawned via the Agent/Task tool; needs the `subagent_type` match + the `Task|Agent` matcher in settings.json
- [ ] `pre-handoff-check.sh` lint/typecheck command is a **bare** interpreter/tool (`ruff check`, `python -m ruff`, `eslint`, `tsc`) that assumes an activated env — the hook runs in a bare shell with no venv/nvm/direnv, so the gate silently fails on every handoff (the failure tosk hit mid-run: bare `python -m ruff` → gate blocked qa). Needs an env-launcher / absolute-env-path command or a defensive interpreter-resolution prologue, per `tpl-skill-guard.md § pre-handoff-check.sh` Bare-shell rule
- [ ] A domain-skill `## Quality Checklist` (or `pre-handoff-check.sh`) names a lint/test command whose target doesn't exist — a `lint` script absent from `package.json`, or a linter/tool not installed — so the dev step (or gate) runs a command that errors on every run (jobzeeker's `zeeker-frontend` checklist ran `npm run lint` with no `lint` script; its hook guarded the call with `grep -q '"lint":'`, the skill rule didn't)
- [ ] `close-out-gate.sh` missing, or settings.json doesn't wire it on PreToolUse Bash
- [ ] `ref-sync-check.sh` warns on every governed commit (no structural/A-D-R filter, no `REF_WATCH`) — alarm fatigue; 15 ignored warnings in one audited session
- [ ] `skill-mark.sh` / `skill-guard.sh` / `dependency-guard.sh` / `package-edit-guard.sh` use session-only markers — subagents inherit each other's skill marks; needs the transcript-basename agent scope (all four derivations must match)
- [ ] Any recorder hook (`ref-sync-check.sh`, `skill-mark.sh`, `post-commit.sh`, or a project-added recorder) can exit non-zero on a success path — successful commands then surface as errors
- [ ] `pre-handoff-check.sh` contains Gemini remnants (`GEMINI_PROJECT_DIR`, `GEMINI.md`) — Gemini support is dropped
- [ ] `governed-paths.conf` lacks `REF_WATCH` or the per-skill self-ownership entries (`'^\.claude/skills/<PREFIX>-<name>/:<PREFIX>-<name>'` before the `.claude/skills/` catch-all)
- [ ] `deploy-config.yaml` serve-envs whose plain `run` starts a component wired to prod (or unusable headless) lack a `stack:` block — QA then has no verifiable target; derive per install § Populate deploy-config.yaml (never invent seed/auth)
- [ ] `CLAUDE.md` `## Agents` lacks the routing paragraph (pipeline-shaped work → `/code`/`/fix`; iterative rounds → `/tweak`) or `## Secrets` is missing
- [ ] Domain skill `## Quality Checklist` sections lack the non-interactive rule **or** the standalone-test rule (a new test file must pass alone in a clean interpreter — order-dependent tests shipped to QA cause salvage loops)
- [ ] `.claude/commands/code.md` / `fix.md` Step 0 confirm is a separate AskUserQuestion from the Step 0.5 capture (up to three sequential 60s-timeout gates per run) or Step 0 doesn't stash pre-existing WIP (the pre-handoff gate then blocks the qa spawn mid-pipeline) — superseded by the single-gate + stash-at-entry form
- [ ] `<PREFIX>-test/SKILL.md` `## Rules` lacks the no-unit-suite-re-run rule (QA re-running dev's full unit suite as a "superset" ignores `regression_mode` and duplicates minutes of test time)
- [ ] `<PREFIX>-qa.md` step 3 lacks the "Verify outcomes, not environments" trust boundary (QA otherwise re-derives dev's environment discovery from scratch, doubling E2E cost)
- [ ] `governed-paths.conf` `PATH_MAP` lacks the `custom-tests.yaml` EXEMPT entry — the Step 1.5 persist gets skill-gate-blocked on every `/code`/`/fix` run
- [ ] `.claude/commands/code.md` / `fix.md` lack the Step 0.5 **Ship** question + `ship_mode` (on push-fires-prod-CI projects the pipeline otherwise always ends parked/held and nothing ever ships without a follow-up "please push"), or their prod step doesn't verify the pre-authorization conditions, or the Gate policy lacks the "ask at the moment of the irreversible action — never infer AFK from an earlier timeout" rule
- [ ] `<PREFIX>-deploy/SKILL.md` gate lacks the **Pre-authorization** clause (`preauth` from the top-level command's Ship answer, verified conditions, `gate: pre-authorized` reporting)
- [ ] `<PREFIX>-design/SKILL.md` still delegates to `frontend-design` — superseded by `ui-ux-pro-max` as the default design-execution skill (only flag when the delegation names `frontend-design`; a deliberately customized skill name is not a gap)
- [ ] **`graph.py` present but untracked** — `git ls-files --error-unmatch .claude/graph/graph.py` fails. It then has no protection: a `git clean`, a `/tidy` discard, or a stray `rm -rf` removes it, and since every call site must fall back silently the workflow keeps running with the graph gone and no signal. Needs `git add`, plus `.claude/graph/__pycache__/` ignored — test with `git check-ignore`, and skip if a repo-wide `__pycache__/` rule already covers it rather than adding a redundant line
- [ ] **Delivery graph absent** — `.claude/graph/graph.py` missing, or present but not byte-identical to the plugin's `shared/graph.py` (a diff means the copy was edited locally instead of the plugin), or `<PREFIX>-graph/SKILL.md` + `references/graph-schema.md` missing, or `governed-paths.conf` lacks the two `.claude/graph/` PATH_MAP entries, or `.gitignore` lacks `.claude/graph/edges.jsonl`
- [ ] **`<PREFIX>-graph/references/graph-schema.md` defines open work as "latest `VERIFIED` edge is not a `pass`"** without excluding `fail` — that reads as a sufficient entry condition, so a failing verification looks like standing open work. `open_verifications` never surfaces one: the two routes in are a `DEFERRED` edge or a latest `blocked`, and not-a-`pass` is only the discharge rule applied to both. Needs the corrected paragraph
- [ ] **Roadmap items have no `**Id:**`** — the delivery log's `**Addresses:**` line has nothing stable to cite, so the roadmap↔delivery link survives only as an untraceable status flip. Needs the format-line update plus a one-off backfill assigning a permanent id to every existing item
- [ ] **Delivery log lacks `**Addresses:**` / `**Decisions:**`** in the `<PREFIX>-log` entry format, field guidance, or Quality Checklist — the roadmap link and gate provenance (`user` vs `timeout` vs `pilot-auto`) are then recorded nowhere durable; also `<PREFIX>-log` Process does not run `graph.py build` after committing the entry, so entries never become queryable
- [ ] **`<PREFIX>-log` derives `**Skills:**` from the session transcript** — grep for `PROJECT_ENCODED` or `~/.claude/projects/*.jsonl` in `<PREFIX>-log/SKILL.md`. That source reads only the parent session and structurally cannot see skills loaded inside the `<PREFIX>-dev`/`<PREFIX>-qa` subagents; measured recall for `<PREFIX>-review` — a skill qa runs on *every* task — was 27–64% across three real installs, and the path is blocked outright on machines with a secret guard over `~/.claude/`. Needs replacing with the `/tmp/<PREFIX>-skills-*` marker union. Also drop the now-orphaned `<PROJECT_ENCODED>` substitution note
- [ ] **`custom-tests.yaml` lacks the `last:` outcome block** in the schema (`custom-tests.md`) or the `<PREFIX>-test` execution protocol doesn't write it — a verification blocked for six consecutive runs is then indistinguishable from one that has never run
- [ ] **Graph call sites unwired** — any of: `<PREFIX>-test` prior-selection still reads the whole `custom-tests.yaml` instead of `graph.py covers`, `<PREFIX>-dev` step 1 lacks `graph.py blast`, `<PREFIX>-pm` lacks step 1.5 (`roadmap-open` **before** the log write — resolving after it means `Addresses:` can't be populated), `<PREFIX>-debug` Phase 1 lacks `graph.py history`, or `/code`+`/fix` Step 0 lacks the `open-deferrals` surface. Each site must also state a fallback for a missing or failing script
- [ ] **pm prompt lacks `Decisions:`** in `/code` / `/fix` Step 3 and `/pilot` step 5 — pm then has no source for the log's `**Decisions:**` line, and `/pilot`'s autonomous branches must be labelled `(pilot-auto)`, never `(user)`
- [ ] `.claude/commands/pilot.md` missing or lacks any of: the "Autonomy contract" section, the single mission gate (Confirm + Ship in one AskUserQuestion), per-task lane routing (`pipeline` / `tweak`), the "Split broad tasks" decompose rule (a uniform change across ~N targets bundled as one task balloons the dev agent — needs chunking or a `[large]` gate tag), the auto-accepted-deferral labeling, or the mission-report Done section — the autonomous multi-task lane; also `CLAUDE.md` `## Agents` routing paragraph lacks the `/pilot` sentence
- [ ] **`/pilot` lanes are hardcoded, or it has no resource ledger / parked-verdict rule** — grep `pilot.md` for `pilot-lane:`, `Resource ledger`, and `Parked verdicts`. Without the first, a project's own command types (a measurement protocol, a paced experiment) can only be routed by improvising in-prompt, because the lane set is frozen at `pipeline`/`tweak`. Without the second, a spend cap is chat-granted with nothing tracking it. Without the third, a human-gated keep/ship verdict produced mid-run is either auto-decided or dropped. Needs the current `tpl-commands.md § /pilot`: frontmatter lane discovery, the `--grant` flag + Budget gate question + ledger, and the parked-verdict contract — plus the `<gate>=parked (human-gated)` value in `<PREFIX>-log`'s `Decisions:` guidance
- [ ] **No foreground-wait rule / salvage misses a parked agent** — grep `<PREFIX>-dev.md` step 4 for `Wait in the foreground` and `code.md`/`fix.md` for `anything that is not a `## Handoff` block`. Without both, a subagent that backgrounds a long deploy watch (`gh run watch`, a poll loop) and ends its turn stalls the run **silently**: the background task's completion is delivered to the orchestrator, not to the dormant agent, so nothing wakes it, and the old salvage trigger ("dies without a `## Handoff`") never fires because the agent did not die. Observed on a real `/fix` — dev deployed, backgrounded a 15-minute watch, returned an interim status, and the pipeline parked at the qa spawn with no error. Needs the step-4 foreground-wait paragraph + the "never end a turn without this block" clause in **both** `<PREFIX>-dev` and `<PREFIX>-qa` Response Requirements, and the widened salvage trigger in `code.md`/`fix.md`
- [ ] **Verifications derived from the diff, not from an acceptance statement** — grep `code.md`/`fix.md` Step 0.5 for `acceptance statement` and `end-state candidate is not optional`. Without it the gate synthesizes candidates from the change, so they assert that the parts exist rather than that the journey ends anywhere: a feature ships with its control wired, every check green, and the user landing nowhere. Observed on a real `/fix` — the reported symptom ("clicking does nothing") was fixed, a CTA was added, the CTA was never pressed, and QA verified the assertions it was handed. Needs, from `tpl-commands.md § Step 0.5`: the acceptance statement written first, candidates derived as clauses of it, the mandatory end-state (E2E) candidate, and the Verify question leading with `Done means: <statement>`; plus the `Done means:` line in the dev and qa prompts, `<PREFIX>-dev` step 3 walk-before-complete, `<PREFIX>-qa` step 4.5 + the end-state `blocked` mapping, and the `/pilot` per-task acceptance statement
- [ ] **An unproven journey can be deferred** — grep `code.md`/`fix.md` for `This is the only case that blocks` and `Prod walk (run when Step 2 carried`, and `<PREFIX>-qa.md` step 5 for `an environment that could reach it exists`. Without them, `signed-off-with-deferrals` absorbs the one check that proves the feature: QA reports clean-except-environment, the command offers a routine deferral, and an untested feature ships with a walk-it-yourself checklist. The routing is by **declared project fact, read from `deploy-config.yaml`, not judgment** — and getting this wrong in the other direction is worse than not having it: a blanket "end state can never be deferred" makes every task fail on a prod-only component. Three branches: a non-prod env exists and the journey still wasn't walked → block (the only blocking case); no non-prod env declared → carry `prod-walk:` to the prod-deploy step and walk it there at top level; out-of-band trigger (schedule, webhook) → defer with named trigger + expected observable + where to look. Needs all three branches, the post-deploy prod walk, and the matching qa mapping
- [ ] **`last:` guidance doesn't rule out a vacuous pass** — grep `<PREFIX>-test/references/custom-tests.md` for `not that nothing went wrong`. **Grep the reference, not `SKILL.md`:** the rule belongs to `## Record the outcome`, which lives in the reference (Rule 5 — SKILL.md is what + when + pointer), so a `SKILL.md` grep reports this gap forever on installs that already have it, and applying it there duplicates a rule that already has a home. Without it, a verification whose condition never arose gets recorded as `pass`: the logs show zero violations, the run looks clean, and the assertion was never exercised. A `pass` is the only thing that closes a deferral, so this **discharges it permanently** and the invariant is never proven — silent, and unrecoverable without someone remembering. Seen on a real deferral read-out where three checks ("at most once", write-order independence, no-starvation) were all vacuous because no refresh was due; a human caught it, an agent would not have. Needs the "pass means the assertion was exercised and held" paragraph from `tpl-lifecycle.md § <PREFIX>-test → Record the outcome`, plus the matching clause on the open-deferral definition
- [ ] **Repeat deferrals never escalate** — grep `code.md`/`fix.md` Step 0 for `3+ open deferrals`. The open-deferrals surface is report-only, so one missing test environment can block five checks across three deploys while each run reports it and moves on. Needs the recurrence clause: 3+ deferrals naming one blocker, or one verification deferred 3+ times, asks whether to fix the blocker first
- [ ] **Salvage treats SendMessage and re-spawn as interchangeable** — grep `code.md`/`fix.md` for `resume mechanisms are not interchangeable`. If the protocol says "continue via SendMessage **or** re-spawn it" with no distinction, the retry leg silently leaves the foreground contract: a fresh `Agent` spawn honours `run_in_background: false`, but **SendMessage to a completed agent always resumes it detached** — it returns immediately ("had no active task; resumed from transcript in the background") and hands control straight back. That return carries no information about the work, yet looks exactly like the interim return the same protocol treats as a stall, so the top level either re-triggers salvage on a healthy agent or reports a parked step as done — on precisely the leg (QA blocks → dev fixes) where a stalled agent is hardest to spot. Observed on a real `/fix`. SendMessage stays the preferred mechanism (context preservation returned the fix as a 16-tool-call delta rather than a re-derivation); what's missing is the contract: **resume, end the turn, wait for the task notification, and never infer state from the resume call**. Needs the two-bullet mechanism split from `tpl-commands.md § Salvage protocol` in both `code.md` and `fix.md`
- [ ] **An external roadmap mirror survives in the agents** — grep `<PREFIX>-dev.md` step 1.5 and `<PREFIX>-pm.md` step 2.5 for `the only roadmap store` — its absence in either is the finding. **Then sweep wider**: `grep -rniE 'notion|mcp__.*(create|update)-page' .claude/` catches mirror calls a *project* added outside the two template sites. One real install carried them in its log and test skills too (4 sites, one dead MCP), and a check scoped to the templates would have declared it clean while three of the four kept firing. The mirror is **removed from the workflow**, not made optional: any surviving wording is an instruction to attempt a call and report its outcome, and on a session with no such MCP server both agents emitted "page NOT created — no MCP tool available" into their handoffs, so every delivery closed with a phantom manual-sync note for a mirror the project never had. Reworded "skip silently" variants are equally stale — the step should not exist at all. Needs both clauses **deleted** and replaced by the one-line `docs/roadmap.md` is the only roadmap store sentence from `tpl-agents.md`. Also clear any roadmap item tracking the old reporting bug (`notion-mirror-reports-own-absence`) — it is resolved by removal
- [ ] **Marker scope described as agent-scoped, or `<PREFIX>-log` lacks the marker-evidence caveat** — grep `<PREFIX>-log/SKILL.md` for `never evidence a command`, and the install/upgrade docs for `agent-scoped`. Markers are session-scoped (see below) and record only what the Skill tool invoked, so a user-typed slash command can leave no trace: concluding "that command never ran" from marker contents is unsound, and was gotten wrong on a real `/pilot` mission whose marker held the dispatched lane but not `pilot`. Needs the caveat paragraph from `tpl-lifecycle.md § tosk-log` Process step 3, plus `session-scoped` wording wherever `agent-scoped` survives
- [ ] **Skill markers keyed per agent** — grep `.claude/hooks/*.sh` for the assignment `TRANSCRIPT=` (not the bare name `transcript_path`, which the *corrected* comment mentions on purpose). If `skill-mark.sh`, `skill-guard.sh`, `dependency-guard.sh` or `package-edit-guard.sh` derive an `AGENT` suffix from it, the key is `${SESSION_ID}-${SESSION_ID}`: subagents inherit the parent's `transcript_path`, so `basename` returns the session id and the suffix is provably always a duplicate (12/12 real markers had identical halves). Harmless at runtime — writer and readers agree — but the accompanying comment claims per-agent isolation the gate does not have, and that false claim invites a "fix" that would silently break `<PREFIX>-log`'s `**Skills:**` derivation, which *requires* session scope to see subagent-only skills. Needs the four scripts keyed on `${SESSION_ID}` alone, plus the corrected comment from `tpl-skill-guard.md § skill-guard.sh`
- [ ] **`<PREFIX>-dev` step 1.5 does not tell the writer to match the roadmap's existing metadata convention** — roadmaps in the wild write fields as list items, bare, or packed several to a line, and the parser reads all three. Without the rule, each writer picks its own form, and one upgrade agent reformatted 390 lines to "normalise" a file that was never broken. Needs the *match the file's convention, never reformat existing items* clause from `tpl-agents.md § tosk-dev` step 1.5. **The upgrade itself must never propose reformatting an existing roadmap** — the only sanctioned roadmap edit is the one-off `**Id:**` backfill
- [ ] **The gate makes the user assemble the verifications, and asks the regression scope before the diff exists** — grep `code.md`/`fix.md` for `header: "Verify"` and `Run full regression after dev as well?`; the presence of either is the finding. The command already derives the typed set from the acceptance statement, so the picker asks the user to choose between checks it just wrote — and a picker is how the end-state check gets dropped, one deselection at a time. The regression question is worse than redundant: it is asked at Step 0.5, **before `<PREFIX>-dev` has run**, so there is no changed-path list and the answer is a guess about a diff nobody has made. Needs, as one unit: Step 0.5 collapsed to Confirm (+ Ship) with the plan of record in the question text and the four-row "Other" disposition table; the narrowed plan shortcut (Confirm may only be omitted for a plan that carries its own Verification/Acceptance section — otherwise `--prod` leaves a zero-question gate); the `--regression full|smart` flag with `regression_mode: auto` as the default; the qa `auto` invocation mode + the resolved scope on its `Tests:` line; `custom-tests.md § Regression scope`; and `agent` in the `<PREFIX>-log` `Decisions:` vocabulary
- [ ] **The end-state verification can be narrowed away, or a blocked one carries no reason** — grep `<PREFIX>-test/SKILL.md` tier table for `non-negotiable`, `custom-tests.md` for `reason:`, `<PREFIX>-qa.md` `mode: retest` for `including step 4.5`, and `tweak.md` for the end-state walk. Without the first, a `smart` scope or a retest can quietly drop the one check that proves the feature — the tier rules say "this task's verifications always" but nothing forbids trading the slow E2E for time. Without the `reason` field the replacement outcome is useless: `graph.py` reads `last.reason` and `/code` Step 0 quotes it, but the schema block and the write instruction never told anyone to write it, so every blocked row surfaces as a name with no way to act on it
- [ ] **`/pilot` auto-accepts every deferral and never walks a prod-only journey** — grep `pilot.md` for `prod-walk` (`/code` and `/fix` carry it twice each; a `pilot.md` with zero is the finding). `/code` routes a deferral by declared environment shape — block / carry to the prod walk / defer on a named out-of-band trigger — but the mission lane collapses all three into "auto-accept", so a prod-only end state is deferred, the mission then **deploys to prod at close-out**, and the one moment that could have proven it passes unused. Observed on a real `/roadmap` mission: two prod-only checks deferred, prod deployed, both still `blocked` afterwards. Worse in the other direction too — a journey that a *non-prod* env could have walked is auto-accepted with nobody present to notice, which is the case `/code` blocks on. Needs the three-branch routing in the `signed-off-with-deferrals` branch, the `(a2) Prod walk` close-out step, the scorecard `Prod walk` row, and the per-deferral route in report item 4
- [ ] **`/roadmap` dispatches one item, re-derives its own rank rule, and advertises a filter it never applies** — grep `roadmap.md` for `roadmap-open`, `--items`, and `§ Rank`, and `pilot.md` for `--items` and `roadmap.md § Rank`. The old `/roadmap` reads the file directly, ranks the top 3 by priority-then-category, and hands exactly one item to `/code`/`/fix` — so a user with eight open items runs eight gates, and the small ones can never reach the tweak lane because `/roadmap` has no lane concept. Meanwhile `/pilot` ranks the same file by priority-then-dependency: two rank rules for one question, which is the duplicated-instruction failure the design contract forbids, and the divergence is invisible from inside either file. The advertised `/roadmap <filter>` has never been implemented — the Usage block promises it and no step reads `$ARGUMENTS`. Needs: `roadmap-open` (with the fallback) as the ranking input, the merged priority → dependency → category rule living in `roadmap.md § Rank` with `pilot.md` citing it, `--items` as the handover, the 3-way route (mission / single item / none), and the filter actually applied
- [ ] **Delivery-log field set not marked closed** — `<PREFIX>-log/SKILL.md` field guidance lacks the "field set above is closed — do not invent new fields" rule, or the entry format carries a field outside the closed set (e.g. `**Follow-ups:**`, observed in the wild restating a roadmap item it had just created). The projector ignores unknown fields silently, so an invented field reads as recorded but contributes nothing. Leftover scope belongs in `docs/roadmap.md`, unrunnable verifications in `**UAT-deferred:**`. Existing entries are **not** rewritten — the rule applies from the next entry on
- [ ] **`<PREFIX>-design` rules on values but not on patterns or icons** — grep `.claude/skills/<PREFIX>-design/` for `ux-patterns.md`; its absence is the finding. Without it every surface is designed in isolation against `design-tokens.md`, so the app grows two answers to one question — a second confirm dialog, a fourth empty state, a nav entry that behaves unlike its neighbours — and nothing ever looks sideways at the rest of the app. It is the UI equivalent of running a task's checks with no prior-selection. The same gap leaves icons to whoever is writing the component, and path geometry composed from a description is reliably logical and reliably ugly. Needs, as one unit: `references/ux-patterns.md` (Inventory + consistency sweep + Iconography); the widened MUST-invoke list and the two named external skills (design intelligence + asset generation, either replaceable) in `<PREFIX>-design/SKILL.md`; the interaction-pattern and icon triggers in the frontend skill's `## Visual Decisions` block; and the `ux-patterns.md` branch in `<PREFIX>-review`'s Read Map, so QA re-checks at review what design settled at build time. Skip entirely on installs with no `<PREFIX>-design`.

---

## Step 6 — Wait for explicit user confirmation

Do not proceed until the user confirms which gaps to apply.

---

## Step 7 — Apply only the confirmed fixes

Each step is idempotent — re-running the upgrade is safe.

- **Create `<PREFIX>-deploy` lifecycle skill**: if `.claude/skills/<PREFIX>-deploy/SKILL.md` does not exist, create it from `../../shared/tpl-lifecycle.md § tosk-deploy/SKILL.md` (substituting `<PROJECT>` and `<PREFIX>`). Idempotent.
- **Migrate `deploy-config.yaml`**: if `<LEGACY_DEPLOY_OWNER>/references/deploy-config.yaml` exists, move it to `.claude/skills/<PREFIX>-deploy/references/deploy-config.yaml` (preserving content). If no yaml exists yet, generate one following install Phase 2's "Populate deploy-config.yaml" sub-step. Idempotent.
- **Strip `## Deployment` section** from every domain skill SKILL.md that has it (deploy logic lives only in `<PREFIX>-deploy/SKILL.md`). Remove the section block; leave the rest of the file untouched. Idempotent.
- **Re-point `PATH_MAP`** in `governed-paths.conf`: if any `'PATTERN:SKILL'` entry maps deploy-mechanism paths to `<LEGACY_DEPLOY_OWNER>` (and that name is not `<PREFIX>-deploy`), change the owner to `<PREFIX>-deploy`. Idempotent.
- **Simplify `<PREFIX>-dev.md` step 4**: replace any inline yaml-handling rules (`verify: local`/`cloud:<env>` etc.) with the one-line delegation from `../../shared/tpl-agents.md § tosk-dev` step 4. Idempotent — skip if step 4 is already a single delegation paragraph.
- **Install the delivery graph**: copy `../../shared/graph.py` to `.claude/graph/graph.py` **verbatim** (overwrite any existing copy — the plugin is the source of truth; if the local copy differed, report the diff so a genuine local fix can be moved into the plugin). Create `<PREFIX>-graph/SKILL.md` + `references/graph-schema.md` from `tpl-lifecycle.md § tosk-graph`. Add `'^\.claude/graph/edges\.jsonl$:EXEMPT'` then `'^\.claude/graph/:<PREFIX>-graph'` to `PATH_MAP` before the `'^\.claude/:OPEN'` catch-all, and `.claude/graph/edges.jsonl` to `.gitignore`. Idempotent.
- **Correct the open-work definition** in `<PREFIX>-graph/references/graph-schema.md`: replace the paragraph that opens "A verification is **open work** when its latest `VERIFIED` edge is not a `pass`" with the form in `../../shared/tpl-lifecycle.md § tosk-graph` — the two routes in (`DEFERRED` edge, or latest `VERIFIED` with `status: blocked`), a later `pass` as the sole closer of both, and a bare `fail` explicitly not a route in. Wording-only; the surrounding blocked/never-run paragraphs stay as they are. Idempotent — skip if the paragraph already says a `fail` is not a route in.
- **Backfill roadmap `**Id:**`**: update the format line in `docs/roadmap.md` to document `**Id:**`, then add a `- **Id:** <kebab-slug>` line to every item that lacks one, derived from its current title and unique within the file. **Assign once and never rewrite** — an id that tracks the title defeats its own purpose. Items already carrying an id are left untouched. Idempotent.
- **Add the delivery-log fields**: add `**Addresses:**` and `**Decisions:**` to the `<PREFIX>-log` entry format, field guidance, and Quality Checklist, and add the `graph.py build` step after the log commit. Existing log entries are **not** rewritten — the fields apply from the next entry on, and the projector treats their absence as "no linkage recorded". Idempotent.
- **Fix the `**Skills:**` derivation**: replace `<PREFIX>-log` Process step 3's transcript grep with the marker-union snippet from `tpl-lifecycle.md § tosk-log`, update the field guidance and Quality Checklist item, and remove the `<PROJECT_ENCODED>` substitution note. Historical entries are **not** rewritten — their `Skills:` lists stay as recorded (and stay incomplete); accuracy applies from the next entry on. Idempotent.
- **Upgrade `/pilot` to pluggable lanes**: re-sync `.claude/commands/pilot.md` from `../../shared/tpl-commands.md § /pilot` (uniform across projects — Rule 3 — so a whole-template re-sync is safe), delivering the frontmatter lane registry, the `--grant` flag + Budget gate + resource ledger, and the parked-verdict contract. Then add the `<gate>=parked (human-gated)` value to `<PREFIX>-log`'s `Decisions:` field guidance. **Existing project commands gain nothing automatically** — a project opts a command into routing by adding a `pilot-lane:` block to its own frontmatter, which this upgrade does **not** author (it doesn't know the project's lanes). Report the discovered lanes, if any, so the user can see whether opting in is wanted. Idempotent — skip if `pilot.md` already contains `pilot-lane:`.
- **Add the foreground-wait rule and widen the salvage trigger**: append the *Wait in the foreground* paragraph to `<PREFIX>-dev.md` step 4, add the "never end a turn without this block" clause to the `## Response Requirements` of **both** `<PREFIX>-dev.md` and `<PREFIX>-qa.md`, and replace the salvage-protocol opening in `code.md`/`fix.md` so it triggers on *any* non-`## Handoff` return rather than only on a dead agent — all from `../../shared/tpl-agents.md` and `tpl-commands.md`. Agent-side and orchestrator-side must land **together**: the rule alone still parks a run whose agent ignores it, and the widened trigger alone lets agents keep making promises they cannot keep. Idempotent — skip if step 4 already contains `Wait in the foreground`.
- **Add the marker-evidence caveat**: append the "a marker is evidence a skill ran; it is never evidence a command did — or didn't" paragraph to `<PREFIX>-log` Process step 3 from `../../shared/tpl-lifecycle.md § tosk-log`, and replace any surviving `agent-scoped` marker wording with `session-scoped`. Idempotent — skip if step 3 already contains `never evidence a command`.
- **Re-key skill markers to session scope**: in `.claude/hooks/skill-mark.sh`, `skill-guard.sh`, `dependency-guard.sh` and `package-edit-guard.sh`, delete the `TRANSCRIPT`/`AGENT` derivation and key the marker on `${SESSION_ID}` alone, with the corrected comment from `../../shared/tpl-skill-guard.md § skill-guard.sh`. Writer and readers must change **together** — a half-applied fix makes every gate look unsatisfied. Markers already on disk keep their doubled names and still match `<PREFIX>-log`'s `/tmp/<PREFIX>-skills-<SESSION>*` glob, so no cleanup is needed. `chmod +x`. Idempotent — skip if none of the four mention `transcript_path`.
- **Add the roadmap-convention rule**: append the *match the file's existing metadata convention, never reformat existing items* clause to `<PREFIX>-dev.md` step 1.5, from `../../shared/tpl-agents.md § tosk-dev`. Do **not** touch `docs/roadmap.md` itself — the only sanctioned roadmap edit in this upgrade is the one-off `**Id:**` backfill. Idempotent — skip if step 1.5 already says "Match the metadata convention".
- **Close the delivery-log field set**: add the "field set above is closed — do not invent new fields" guidance paragraph and the matching Quality Checklist item to `<PREFIX>-log/SKILL.md`, from `../../shared/tpl-lifecycle.md § tosk-log`. If the entry format template carries a field outside the closed set (e.g. `**Follow-ups:**`), remove it from the template and say where that content belongs instead (roadmap item, or `**UAT-deferred:**`). Existing log entries are **not** rewritten. Idempotent — skip if the guidance already contains "field set above is closed".
- **Add `last:` to the verification schema**: document the block in `custom-tests.md` and add the write step + Reference Sync item to `<PREFIX>-test`. Existing entries gain `last:` as they run; a missing block reads as "never run", never as a pass. Idempotent.
- **Wire the graph call sites**: apply the `covers` / `blast` / `roadmap-open` / `history` / `open-deferrals` edits from `tpl-lifecycle.md`, `tpl-agents.md` and `tpl-commands.md`, each with its fallback clause. In `<PREFIX>-pm` this means adding step 1.5 **before** the log write, not amending step 2.5. Add the `Decisions:` line to the pm prompt in `/code`, `/fix` and `/pilot`. Idempotent.
- **Run `python3 .claude/graph/graph.py build`** once at the end — it backfills the whole existing delivery history from records already on disk, so the graph is useful immediately rather than only for future tasks.
- **Update `governed-paths.conf`**: add the `DEPLOY_PATHS='<value>'` line (and the explanatory comment block from `../../shared/tpl-skill-guard.md § governed-paths.conf`) above `PATH_MAP`. Compute `<value>` from the IaC/CI/CD/Build/Deployment paths in `CATEGORY_MAP`. Update the file's header comment to mention `ref-sync-check.sh` as a consumer.
- **Replace `ref-sync-check.sh`** with the conf-sourcing version from `../../shared/tpl-skill-guard.md § ref-sync-check.sh`. `chmod +x`.
- **Add `Commit` step** to `.claude/agents/<PREFIX>-dev.md`: insert step 6 from `../../shared/tpl-agents.md § tosk-dev` between Reference Sync and Hand off, and update Hand off to step 7 with the latest wording. Idempotent — skip if a Commit step already exists.
- **Add `Reference Sync` step** to `.claude/agents/<PREFIX>-qa.md`: insert step 6.5 from `../../shared/tpl-agents.md § tosk-qa` between Sign off and Hand off, and update Hand off to mention Reference Sync. Idempotent — skip if a Reference Sync step already exists.
- **Add `Reference Sync` step** to `.claude/agents/<PREFIX>-pm.md`: insert step 3.5 from `../../shared/tpl-agents.md § tosk-pm` between Docs check and Verify log written. Idempotent — skip if a Reference Sync step already exists.
- **Migrate `/log` → `/wrap`**: if `.claude/commands/log.md` exists, replace its content with the `/wrap` template from `../../shared/tpl-commands.md § /wrap`, then rename the file to `wrap.md`. Idempotent — skip if `wrap.md` already exists.
- **Fix dev Hand off wording** in `.claude/agents/<PREFIX>-dev.md`: replace any legacy `invoke the <PREFIX>-qa agent directly...` wording with the current Hand off line from `../../shared/tpl-agents.md § tosk-dev` step 7. Idempotent — skip if already updated.
- **Fix qa Hand off wording** in `.claude/agents/<PREFIX>-qa.md`: replace any legacy `invoke the <PREFIX>-pm agent directly...` wording with the current Hand off line from `../../shared/tpl-agents.md § tosk-qa` step 7. Idempotent — skip if already updated.
- **Update `<PREFIX>-log/SKILL.md`** entry format only if user opted in: add the `**Deployed:**` line and the corresponding field guidance + Quality Checklist item from `../../shared/tpl-lifecycle.md § tosk-log/SKILL.md`. Idempotent — skip if already present.
- **Add `## Visual Decisions` block to frontend skill** if `<PREFIX>-design` exists and the skill that owns the Frontend category in `CATEGORY_MAP` is missing the section: insert the block from `../../shared/tpl-domain-skill.md § Design delegation block` (substituting `<PREFIX>`) immediately after the `## Architecture` section. Idempotent — skip if a `## Visual Decisions` section already exists.
- **Replace `<PREFIX>-design/SKILL.md`** description and "When this skill MUST be invoked" section with the stricter version from `../../shared/tpl-lifecycle.md § <PREFIX>-design/SKILL.md` (preserving any user customization in `## What this skill owns`, `## References`, and `## Reference Sync`). Idempotent — skip if description already starts with "Visual authority for <PROJECT>. MUST be invoked". This bullet only restores mandatory *language*; widening the skill to interaction patterns and icons is the separate bullet below, which guards on the end state rather than on this phrase — every install has said "MUST be invoked" since long before patterns were in scope.
- **Dedupe verification doc**: if `.claude/skills/<PREFIX>-review/references/verification-before-completion.md` exists, delete it; update `<PREFIX>-review/SKILL.md` `## References` to replace the bullet for that file with `Verification gates: see <PREFIX>-debug/references/verification.md (single source of truth — owned by <PREFIX>-debug)`, and delete the corresponding `## Reference Sync` checklist line. Idempotent.
- **Collapse `<PREFIX>-qa.md` step 3+4 and remove tier table**: replace the separate `Determine test tier` (step 3) and `Test` (step 4) steps with the single delegation step from `../../shared/tpl-agents.md § tosk-qa` step 3. Remove the `## Sign-off criteria` section in full. Renumber subsequent steps (5→4, 6→5, 6.5→5.5, 7→6). Idempotent — skip if step 3 already begins with `Test — invoke`.
- **Gate the Reference Sync step in agents**: replace the unconditional Reference Sync paragraph in `.claude/agents/<PREFIX>-dev.md` step 5, `<PREFIX>-qa.md` step 5.5, and `<PREFIX>-pm.md` step 3.5 with the gated form from `../../shared/tpl-agents.md`. Idempotent — skip if step text already contains `if its `## Reference Sync` checklist exists AND files in its owned paths`.
- **Replace `## What this agent does NOT do` with `## Boundaries`** in all three agents: rewrite the bullet list as a single one-line paragraph using the wording in `../../shared/tpl-agents.md`. Idempotent — skip if a `## Boundaries` section already exists.
- **Trim `<PREFIX>-debug/SKILL.md` to read-map form**: replace the body with the `## Iron Law` + `## Read Map` + `## References` + `## Red Flags` structure from `../../shared/tpl-lifecycle.md § tosk-debug/SKILL.md` and tighten the frontmatter description. Reference files (`systematic-debugging.md`, `root-cause-tracing.md`, `defense-in-depth.md`, `verification.md`) and `scripts/find-polluter.sh` are unchanged. The skill must end up without a `## Reference Sync` section. Idempotent — skip if SKILL.md already contains `## Read Map`. **Also skip as a deliberate customization** (not a gap) if the body is project-tailored or the skill carries project-authored references — then keep its form and keep `## Reference Sync` scoped to exactly the project-authored files (Global Invariant 5 ownership rule).
- **Trim `<PREFIX>-review/SKILL.md` to read-map form**: replace the body with the `## Read Map` + `## References` structure from `../../shared/tpl-lifecycle.md § tosk-review/SKILL.md`. Drop the `## Reference Sync` section entirely. Reference files (`code-review-reception.md`, `requesting-code-review.md`, `issuing-findings.md`) are unchanged or added by the next step. Idempotent — skip if SKILL.md already contains `## Read Map` and lacks `## Reference Sync`. **Also skip as a deliberate customization** under the same carve-out as `<PREFIX>-debug` above.
- **Re-sync the `<PREFIX>-skill` parity invariant**: replace the blanket "every skill must have a Reference Sync checklist" invariant in `<PREFIX>-skill/SKILL.md` with Global Invariant 5 from `../../shared/tpl-lifecycle.md § tosk-skill` (parity rule + static-content exception + content-ownership scoping), and update the sync-checklist parity line if the skill carries one. Idempotent — skip if the invariant already contains "static-content".
- **Add `<PREFIX>-review/references/issuing-findings.md`** if missing: copy the file content from `../../shared/tpl-lifecycle.md § tosk-review/SKILL.md` (the `references/issuing-findings.md` block). Then ensure `<PREFIX>-review/SKILL.md` `## Read Map` includes the `Issuing review findings → references/issuing-findings.md` branch and `## References` lists the file. Idempotent — skip if the file exists and the read-map branch is present.
- **Add `<PREFIX>-review/references/security-review.md`** if missing: copy the file content from `../../shared/tpl-lifecycle.md § tosk-review/SKILL.md` (the `references/security-review.md` block). Then ensure `<PREFIX>-review/SKILL.md` `## Read Map` includes the `Reviewing for defect classes → references/security-review.md` branch, `## References` lists the file, and `references/issuing-findings.md` carries the pointer line to `security-review.md`. Idempotent — skip if the file exists and the read-map branch is present.
- **Add `## Preconditions` section to every domain skill SKILL.md** that has a `## Architecture` section but no `## Preconditions` section: insert the block from `../../shared/tpl-domain-skill.md § Domain skill stub` (the `## Preconditions — Verify Before Writing References` block, verbatim) immediately after the `## Architecture` section's content and before the next section (which may be `## Visual Decisions`, `## Quality Checklist`, or other). Domain skills are identified by the presence of `## Architecture` — lifecycle skills don't have it. Idempotent — skip any skill that already contains `## Preconditions`.
- **Replace `<PREFIX>-deploy/SKILL.md` `## Deployment` section** with the version from `../../shared/tpl-lifecycle.md § tosk-deploy/SKILL.md` (Caller contract with ship-env/serve-env predicates + Fill-in pass + inline gate with context block). Preserve `references/deploy-config.yaml` content as-is. Idempotent — skip if `## Deployment` already contains "ship-env".
- **Migrate `deploy-config.yaml` to the unified env schema**: for each component, move a top-level `local: {run, url, health_path}` block to `envs.local` (insert as the **first** env so it stays the first non-prod target) and rewrite `verify: cloud:<env>` → `verify: <env>` (`verify: local` unchanged). Values are moved verbatim — never invented or overwritten. Idempotent — skip if no component has a top-level `local:` key and no `verify:` contains `cloud:`.
- **Fill missing non-prod envs**: after migration, any component that is locally runnable (a dev/start script exists for it) but declares **no** non-prod env gets an `envs.local` serve-env — derive `run`/`url` from the project manifest per `../install/SKILL.md § Populate deploy-config.yaml`, and batch anything underivable into a single `AskUserQuestion`. Typed UX/E2E verifications resolve to the first non-prod env; without one they block. Idempotent — skip components that already have a non-prod env.
- **Ungate `<PREFIX>-deploy` (remove the model-invocation block)**: if `<PREFIX>-deploy/SKILL.md` frontmatter contains a `disable-model-invocation` line, **delete it** — the deploy skill MUST be model-invokable so the `<PREFIX>-dev` deploy step (`target=non-prod`) and the `/code|/fix --prod` step (`target=prod`) can call it via the Skill tool; the gate blocked exactly those calls (`Error: Skill … cannot be used with Skill tool due to disable-model-invocation`). Also re-sync the frontmatter `description:` to the version in `../../shared/tpl-lifecycle.md § tosk-deploy/SKILL.md` (names the two callers + "not triggered autonomously"). "Never deploy on whim" is enforced by the description + the prod `user_confirm` gate, not a frontmatter gate. Idempotent — skip if frontmatter has no `disable-model-invocation` line **and** the description contains "single source of truth for how it deploys".
- **Update `<PREFIX>-dev.md` step 4** to pass `target=non-prod` and drop the trailing "If a gate is declined or skipped" clause. Use the wording from `../../shared/tpl-agents.md § tosk-dev` step 4. Idempotent — skip if step 4 already contains "target=non-prod".
- **Set `<PREFIX>-pm.md` to `model: sonnet`**: add `model: sonnet` to the frontmatter (from `../../shared/tpl-agents.md § tosk-pm`) — mechanical close-out doesn't need Opus. Idempotent — skip if a `model:` line already exists.
- **Remove `<PREFIX>-pm.md` prod-deploy step + harden the log**: delete the `1.5. Deploy to prod` step (any step referencing `<PREFIX>-deploy` / `target=prod`) and renumber; add the "defining deliverable is the delivery log" framing to the title block, the "core, non-negotiable job" note on the Write-delivery-log step, the step-4 hard-gate wording, and "No deployments" to `## Boundaries` — all from `../../shared/tpl-agents.md § tosk-pm`. Idempotent — skip if pm has no `target=prod` and `## Boundaries` already contains "No deployments".
- **Add the pm deploy-tangent guard to `## Boundaries`**: ensure `<PREFIX>-pm.md` `## Boundaries` contains the explicit anti-deploy guard from `../../shared/tpl-agents.md § tosk-pm` — the "Do not read `deploy-config.yaml`, invoke `<PREFIX>-deploy`, or reason about deploy at all … Your turn ends only when the delivery log is written." sentence. Insert it into the existing `## Boundaries` paragraph if absent. This forecloses the failure where pm follows a stray deploy instinct, burns its turn on deploy reasoning, and never writes the log. Idempotent — skip if `## Boundaries` already contains "Do not read `deploy-config.yaml`".
- **Add `<PREFIX>-pm.md` terminal handoff**: if `<PREFIX>-pm.md` has no `## Response Requirements` section, append one as the final section (after `## Boundaries`) with the `## Handoff` block from `../../shared/tpl-agents.md § tosk-pm` (`Status: complete | blocked` + `Log` / `Docs` / `Reference Sync` / `Notes`), and add the final `Hand off` workflow step pointing to it. Idempotent — skip if `## Response Requirements` already exists.
- **Replace `<PREFIX>-dev.md` step 7 (Hand off)** with the structured `## Handoff` block version from `../../shared/tpl-agents.md § tosk-dev` step 7. Update step 6 (Commit) to handle the no-git-repo case. Idempotent — skip if step 7 already contains "Status: complete | blocked".
- **Replace `<PREFIX>-qa.md` step 2 (Address findings)** with the no-self-patch version from `../../shared/tpl-agents.md § tosk-qa` step 2. Idempotent — skip if step 2 already contains "You do not edit code".
- **Replace `<PREFIX>-qa.md` step 6 (Hand off)** with the structured `## Handoff` block version from `../../shared/tpl-agents.md § tosk-qa` step 6. Idempotent — skip if step 6 already contains "Status: signed-off | blocked".
- **Add the blocked-verification mapping to `<PREFIX>-qa.md` step 5 (Sign off)**: extend the step with the sentences from `../../shared/tpl-agents.md § tosk-qa` step 5 — a blocked verification is not clean → `Status: blocked` with the verification + reason in `Notes:`; never substitute component/unit tests; UAT-deferral is the command's user-gated decision. Idempotent — skip if step 5 already contains "UAT-deferral is the command's".
- **Replace `<PREFIX>-pm.md` step 1 (Verify QA phases)** with the QA-evidence-from-prompt version from `../../shared/tpl-agents.md § tosk-pm` step 1. Idempotent — skip if step 1 already contains `**QA-evidence:**`.
- **Re-sync `code.md` and `fix.md`** from `../../shared/tpl-commands.md § /code` and `§ /fix` in full (commands are uniform across projects — Rule 3 — so a whole-template re-sync is safe and carries no project-specific content). This delivers, in one pass: the `--prod`/`--no-push` flag parse + top-level prod-deploy step; the **Gate policy** section (risk-split timeouts); entry hygiene + plan shortcut; the Step 0.5 capture + UAT-only intent rule; the persist step (single-quoted, symbolic asserts, behavioral-surface `paths`, salvage-tolerant condition); the `stack:`/freshness-aware "Ensure verification stack" step; the salvage protocol + no-top-level-edits rule; the `signed-off-with-deferrals` branch; `Feature commit:`/`UAT-deferred:` in the pm prompt; the "Close out: push + verified scorecard" step; and the evidence-first Done section. Substitute `<PREFIX>`/`<PROJECT>`. The re-sync also delivers the **single gate** (Confirm — carrying the acceptance statement and the derived verification set — plus Ship, in one AskUserQuestion call; no Verify picker, no Regression question), the `--regression` flag with `regression_mode: auto`, `ship_mode` + the prod-step pre-authorization conditions, and the stash-at-entry / restore-at-close-out WIP handling. Idempotent — skip if both files already contain `Gate policy` **and** `verified scorecard` **and** `signed-off-with-deferrals` **and** `ship_mode` **and** `regression_mode: auto`, **and** neither contains `header: "Verify"`. Do **not** guard on `Single gate` alone — that phrase is in every install back to the pre-`auto` shape, so it would skip the files this pass exists to repair.
- **Add `## Invocation modes` to `<PREFIX>-qa.md`** between the title block and `## Workflow`, from `../../shared/tpl-agents.md § tosk-qa § Invocation modes`. Idempotent — skip if `## Invocation modes` already exists.
- **Rewrite `.claude/commands/code.md` Steps 2–3** from `../../shared/tpl-commands.md § /code`: Step 2 must parse dev `## Handoff` Status, branch on qa Status, and re-spawn qa with `mode=retest` after a dev fix; Step 3 must pass `**QA-evidence:**` (qa's full handoff block) to pm. Idempotent — skip if Step 3 already contains "QA-evidence" and Step 2 already contains "mode=retest".
- **Rewrite `.claude/commands/fix.md` Steps 3–4** from `../../shared/tpl-commands.md § /fix` with the same shape (Step 3 = qa branching + retest, Step 4 = QA-evidence to pm). Idempotent — skip if Step 4 already contains "QA-evidence" and Step 3 already contains "mode=retest".
- **Restructure `<PREFIX>-dev.md` Hand off** to the terminal-section form in `../../shared/tpl-agents.md § tosk-dev`: collapse step 7 to the one-line pointer (`emit the ## Handoff block specified in ## Response Requirements below. Stop.`), strip the block format + Use-Status-blocked note from the workflow step, and append a new `## Response Requirements` section as the **last** section of the file (after `## Boundaries`) containing the CRITICAL imperative + block format. Idempotent — skip if a `## Response Requirements` section already exists.
- **Restructure `<PREFIX>-qa.md` Hand off** to the terminal-section form in `../../shared/tpl-agents.md § tosk-qa`: same shape — step 6 becomes the one-line pointer; block format + imperative move to a terminal `## Response Requirements` section after `## Boundaries`. Idempotent — skip if a `## Response Requirements` section already exists.
- **Scaffold / re-sync test reference files**: create `.claude/skills/<PREFIX>-test/references/custom-tests.yaml` (`tests: []` + leading comment) if missing; create-or-re-sync `references/custom-tests.md` from `../../shared/tpl-lifecycle.md § tosk-test`. Idempotent — skip `custom-tests.yaml` if it exists (never overwrite captured data); skip `custom-tests.md` if it already contains "first non-prod env url". The re-sync delivers the running-stack rule (C) with the component/unit-test ban, the first-non-prod-env (never prod) UX/E2E resolution, the single-quoted schema (A), the "verification" vocabulary, and the `type: UX|Integration|E2E` taxonomy. If `custom-tests.yaml` has entries with a `surface:` key, rename it to `type:` and map `api`/`data`→`Integration`, `ui`→`E2E` (ships `tests: []`, so usually a no-op).
- **Restructure `<PREFIX>-test/references/test-commands.md`**: if it lacks the three headings, insert `## Smoke`, `## Regression`, and `## Functional Feature Subjects` from the `tosk-test` template, **preserving all existing content** — move existing curl/smoke snippets under `## Smoke` and any subject-query snippets under `## Functional Feature Subjects`; leave everything else in place. Idempotent — skip if all three headings already present.
- **Rework `<PREFIX>-test/SKILL.md`**: replace the body with the 3-tier form from `../../shared/tpl-lifecycle.md § tosk-test` — `## Test Plan` tier table, `## Smoke` / `## Functional Feature Tests` / `## Regression` sections, the Rule-5 pointer to `references/custom-tests.md`, and `## References` ↔ `## Reference Sync` listing the four reference files. Delete the `## E2E Browser Tests` section and the `<FRONTEND_PATHS>`→E2E row from `## Test Plan` (its screenshot/`open` rules now live in `custom-tests.md`). Idempotent — skip if `## Test Plan` already lists `Functional Feature` and `Regression` tiers, the body says "per-task verifications", the `## Regression` section carries the "not yet captured" anti-duplication principle, and no `## E2E Browser Tests` section exists.
- **Update `<PREFIX>-test/references/sync-checklist.md`**: replace the "E2E browser target URL" line with the "Smoke or Regression command no longer matches" line, and add the `## Update references/custom-tests.yaml when:` block from the `tosk-test` template. Idempotent — skip if a `custom-tests.yaml` section already exists.
- **Add `regression_mode` to `<PREFIX>-qa.md`**: add the `regression_mode` bullets to `## Invocation modes` and update step 3 to forward `regression_mode` + new functional-feature entry names + the dev `Files changed:` paths to `<PREFIX>-test`, from `../../shared/tpl-agents.md § tosk-qa`. Idempotent — skip if `## Invocation modes` already mentions `regression_mode`.
- **Upgrade `<PREFIX>-qa.md` to the three-status vocabulary**: re-sync step 1 (per-file diff reads), step 2 (source/non-source class split), step 5 (deferral mapping), and the `## Response Requirements` block (`signed-off | signed-off-with-deferrals | blocked` + `Evidence:` + `UAT-deferred:` + the never-relabel rule) from `../../shared/tpl-agents.md § tosk-qa`. Idempotent — skip if the handoff block already contains `signed-off-with-deferrals`.
- **Upgrade `<PREFIX>-pm.md` for deferrals + feature commit**: re-sync step 1 (accept both signed-off statuses), step 2 (feature-commit hash rule + `UAT-deferred:` carry), and step 2.5 (required roadmap advancement) from `../../shared/tpl-agents.md § tosk-pm`. Idempotent — skip if step 1 already contains `signed-off-with-deferrals`.
- **Add the non-interactive rule to `<PREFIX>-dev.md` step 3** from `../../shared/tpl-agents.md § tosk-dev`. Idempotent — skip if step 3 already contains "non-interactively".
- **Create `tweak.md` and `revert.md`** from `../../shared/tpl-commands.md § /tweak` and `§ /revert`, substituting `<PREFIX>`/`<PROJECT>`. Idempotent — skip each file that exists.
- **Create `tidy.md`** from `../../shared/tpl-commands.md § /tidy`, substituting `<PREFIX>`/`<PROJECT>`. The `code.md` / `fix.md` / `pilot.md` entry-hygiene routing to `/tidy` arrives with their full re-sync above — no separate edit. Also ensure the `CLAUDE.md` `## Agents` routing paragraph carries the `/tidy` sentence (from `../../shared/tpl-domain-skill.md § Project file sections`). Idempotent — skip the file if `tidy.md` exists; skip the routing sentence if `## Agents` already mentions `/tidy`.
- **Create or re-sync `pilot.md`** from `../../shared/tpl-commands.md § /pilot` in full (commands are uniform across projects — Rule 3), substituting `<PREFIX>`/`<PROJECT>`. Also ensure the `CLAUDE.md` `## Agents` routing paragraph carries the `/pilot` sentence (from `../../shared/tpl-domain-skill.md § Project file sections`). The re-sync also delivers the deferral routing by declared environment shape and the `(a2) Prod walk` close-out — without them a mission defers a prod-only journey, deploys to prod, and never walks it. Idempotent — skip the file if it already contains "Autonomy contract", "mission report", "Split broad tasks" (re-syncs installs that predate split-detection), `--regression` (re-syncs installs that predate the mission-scope flag), and `prod-walk` (re-syncs installs whose deferral branch still auto-accepts everything); skip the routing sentence if `## Agents` already mentions `/pilot`.
- **Re-sync `wrap.md`** from `../../shared/tpl-commands.md § /wrap`: the conditional Step 0 review, the `--no-push` flag, and the Step 4 push + scorecard. Idempotent — skip if wrap.md contains both `## Step 0 — Review` and `## Step 4 — Push + scorecard`.
- **Add `## Push policy` + gate parking + non-prod early exit + the Pre-authorization clause to `<PREFIX>-deploy/SKILL.md`** from `../../shared/tpl-lifecycle.md § tosk-deploy/SKILL.md`. Idempotent — skip if the file contains `## Push policy`, `gate: unanswered — parked`, and `Pre-authorization`.
- **Add the Freshness rule + Evidence trace to `<PREFIX>-test/references/custom-tests.md`** from `../../shared/tpl-lifecycle.md § tosk-test` (insert after the Running-stack rule). Idempotent — skip if the file contains "Freshness rule".
- **Fix `<PREFIX>-log/SKILL.md` hash semantics**: re-sync Process steps 1–2, the entry format (`UAT-deferred:` line), field guidance, and Quality Checklist item 2 from `../../shared/tpl-lifecycle.md § tosk-log/SKILL.md`. Idempotent — skip if Process step 1 contains "primary feature/fix commit".
- **Rewire `pre-handoff-check.sh`**: replace with the template version (matches `tool_input.skill` OR `tool_input.subagent_type`; no Gemini branches) and add the `Task|Agent` PreToolUse matcher to settings.json. `chmod +x`. Idempotent — skip if the script contains `subagent_type` and settings.json has the matcher.
- **Fix a bare-shell lint/typecheck command in `pre-handoff-check.sh`**: if the hook's `<LINT_CMD>`/`<TYPECHECK_CMD>` is a bare interpreter/tool that assumes an activated env (`ruff check`, `python -m ruff`, `eslint`, `tsc` with no env-launcher, absolute env path, or interpreter-resolution prologue), rewrite it per `../../shared/tpl-skill-guard.md § pre-handoff-check.sh` Bare-shell rule — prefer the project's env-launcher (`uv run`, `poetry run`, `pnpm exec`, `npx --no-install`) or an absolute env path (`.venv/bin/…`, `node_modules/.bin/…`); when the interpreter path may or may not exist, add the defensive resolution prologue from the template. Verify the rewritten command actually exits 0 from a bare shell against the clean tree before finishing. `chmod +x`. Idempotent — skip if the lint line already uses an env-launcher, an absolute env path, or a resolved `$PY`/`$BIN` variable.
- **Fix an unwired lint/test command in a domain-skill `## Quality Checklist`**: if a rule names a command whose target doesn't resolve — a `package.json` script that's absent from `scripts`, or a linter/tool that isn't an installed dependency — replace it with the project's real check (the actual wired script, or the type gate that stands in for lint, e.g. an `astro build` / `tsc` step) or remove the rule; never leave a check that errors on every run. Cross-check the hook: the Quality Checklist lint command and `pre-handoff-check.sh`'s `<LINT_CMD>` must agree (same verified command, or both absent). Verify the retained command exits cleanly against the clean tree before finishing. Idempotent — skip if every Quality Checklist command already resolves.
- **Create `close-out-gate.sh`** from the template, `chmod +x`, and wire it under the PreToolUse Bash matcher in settings.json. Idempotent — skip if the file exists and is wired.
- **Replace `ref-sync-check.sh`** with the structural/`REF_WATCH`-filtered version. `chmod +x`. Idempotent — skip if the script contains `REF_WATCH`.
- **Audit recorder exit codes**: `ref-sync-check.sh`, `skill-mark.sh`, `post-commit.sh`, and any project-added recorder hook must end in an unconditional `exit 0`; fix any success path that can exit non-zero (a grep finding nothing must not fail the hook).
- **Add `REF_WATCH` + self-ownership entries to `governed-paths.conf`**: add the `REF_WATCH='<value>'` line (derive from the category map's Backend/Auth rows, `''` if unclear) and one `'^\.claude/skills/<PREFIX>-<name>/:<PREFIX>-<name>'` entry per installed skill before the `.claude/skills/` catch-all. Idempotent — skip what already exists.
- **Derive `stack:` blocks in `deploy-config.yaml`** for serve-envs whose plain `run` cannot produce a verifiable target (prod-wired env vars, no headless auth/data) — per `../install/SKILL.md § Populate deploy-config.yaml` (`envs.local.stack`). Propose to the user; never invent seed/auth values. Idempotent — skip envs that already declare `stack:` or verifiably don't need it.
- **Upsert the CLAUDE.md routing + Secrets sections** from `../../shared/tpl-domain-skill.md § Project file sections` (`## Agents` routing paragraph, `## Secrets`). Idempotent — skip what already exists.
- **Add the non-interactive + standalone-test rules to every domain skill `## Quality Checklist`** from `../../shared/tpl-domain-skill.md § Domain skill stub`. Idempotent — skip skills that already contain both "non-interactive" and "standalone".
- **Add the no-unit-suite-re-run rule to `<PREFIX>-test/SKILL.md` `## Rules`** from `../../shared/tpl-lifecycle.md § tosk-test`. Idempotent — skip if Rules already contains "Never re-run domain unit".
- **Fold the acceptance statement into Step 0.5** in `code.md` and `fix.md` per `../../shared/tpl-commands.md § Step 0.5` (statement first, the verification set derived from it, mandatory end-state verification, `Done means:` in the Confirm question and in the dev/qa prompts), plus `<PREFIX>-dev.md` step 3 walk-before-complete, `<PREFIX>-qa.md` step 4.5 and its step-5 end-state `blocked` mapping, the deferral-gate end-state pre-check, the Step 0 recurrence clause, and the `/pilot` per-task acceptance statement. Apply as one unit — the statement is worthless if dev doesn't build to it or qa doesn't walk it. **Idempotent — skip a file only when its dev *and* qa prompt blocks both contain `Done means:`.** Do not gate on the presence of "acceptance statement": a command can capture the statement at the gate, reference it in its deferral branch, and still never pass it to either agent — that was the real state of `/fix`, and a presence check would have skipped the file forever rather than repairing it. Check the thing that must be true at the end, not a phrase that appears when the work is half done.
- **Add the vacuous-pass rule to `<PREFIX>-test/references/custom-tests.md`**: the "pass means the assertion was exercised and held" paragraph in its `## Record the outcome` section, plus the closing clause on the open-deferral definition, from `../../shared/tpl-lifecycle.md § tosk-test` (inside the `references/custom-tests.md` block). **Not `SKILL.md`** — that is where the rule was mistakenly targeted before, which made this bullet fire on every install that already had it and would have duplicated the rule out of the reference that owns it. Idempotent — skip if `custom-tests.md` already contains "not that nothing went wrong".
- **Split the salvage resume mechanisms** in `code.md` and `fix.md`: replace the "continue the same agent via SendMessage or re-spawn it" sentence with the two-bullet SendMessage/re-spawn split plus the "never infer state from the resume call" paragraph from `../../shared/tpl-commands.md § Salvage protocol`. Idempotent — skip files that already contain "resume mechanisms are not interchangeable".
- **Delete the external roadmap mirror** from `<PREFIX>-dev.md` step 1.5 and `<PREFIX>-pm.md` step 2.5, replacing each with the one-line "`docs/roadmap.md` is the only roadmap store" sentence from `../../shared/tpl-agents.md`. Remove the whole sub-step/clause — do not reword it to skip-silently, which is the previous, now-stale fix. **Remove every site the wider sweep found, not just the two template ones** — a project-added mirror call is the project's own content, so deleting it is ordinary cleanup rather than a hand-patch, and leaving it keeps the same dead call firing from a file the template will never touch. If `docs/roadmap.md` carries an open item for the old reporting bug (`notion-mirror-reports-own-absence` or equivalent), flip it to `done · YYYY-MM-DD` in the same pass, since removal resolves it — but only once **all** sites are gone, or the item is closed while the symptom still occurs. Idempotent — skip a file only when the step already contains "the only roadmap store". Do **not** guard on the absence of "notion"/"mirror": the replacement sentence itself says "never mirror", so a keyword guard would re-fire on its own output.
- **Collapse the gate to Confirm (+ Ship) and defer the regression scope to the blast radius.** Apply as **one unit** — a gate that stops asking while qa still demands a pinned scope strands the run. Re-sync Step 0.5 in `code.md` and `fix.md` per `../../shared/tpl-commands.md` (plan of record in the Confirm text, four-row "Other" disposition table, no-behavioral-surface exemption, narrowed plan shortcut, `--regression` in the flag parse, `regression_mode: auto`, retest passing back the *resolved* value); add the `auto` bullet + the both-directions binding rule to `<PREFIX>-qa.md § Invocation modes` and the resolved scope + reason to its `Tests:` handoff field; add `## Regression scope` to `<PREFIX>-test/references/custom-tests.md` and the `auto` rule to `<PREFIX>-test/SKILL.md § Rules`; add `agent` to the `<PREFIX>-log` `Decisions:` `<how>` vocabulary, its `Tests:` guidance, and Quality Checklist item 7. **Idempotent — skip only when all of:** `code.md` and `fix.md` each contain `--regression` **and** `regression_mode: auto` **and** no `header: "Verify"`; `<PREFIX>-qa.md § Invocation modes` mentions `auto`; `custom-tests.md` contains `## Regression scope`; `<PREFIX>-log/SKILL.md` `Decisions:` guidance contains `agent`. Never guard on `Single gate` or `Step 0.5` — both are true in every install predating this change.
- **Turn `/roadmap` into a lane selector** — whole-file re-sync of `roadmap.md` **and** `pilot.md` from `../../shared/tpl-commands.md § /roadmap` and `§ /pilot` (Rule 3 makes a whole-file re-sync safe), substituting `<PREFIX>`/`<PROJECT>`. Apply both together: the merged rank rule lives in `roadmap.md § Rank` and `pilot.md` cites it, so re-syncing one alone either strands the citation or leaves the duplicate rule standing. Also update the `docs/workflow.md` commands section to say `/roadmap` ranks and hands its set to `/pilot`. Idempotent — skip only when `roadmap.md` contains `--items` **and** `roadmap-open` **and** `## Step 2 — Rank`, **and** `pilot.md` contains `--items` **and** `roadmap.md § Rank`.
- **Make the end-state verification non-negotiable** — four edits, one unit: the `non-negotiable` paragraph under `<PREFIX>-test/SKILL.md`'s tier table; `reason:` in the `custom-tests.md` schema block **and** in its `## Record the outcome` write instruction (required on `blocked`/`fail`); `including step 4.5` on `<PREFIX>-qa.md`'s `mode: retest` bullet; and the end-state walk in `tweak.md`'s batched close-out. Do not add verification capture to `/tweak` — that lane persists nothing by design. Idempotent — skip only when all four strings are present.
- **Add the trust boundary to `<PREFIX>-qa.md` step 3** ("Verify outcomes, not environments" sentence) from `../../shared/tpl-agents.md § tosk-qa`. Idempotent — skip if step 3 already contains "Verify outcomes, not environments".
- **Add the `custom-tests.yaml` EXEMPT entry to `governed-paths.conf`** (`'^\.claude/skills/<PREFIX>-test/references/custom-tests\.yaml$:EXEMPT'`, placed with the other EXEMPT entries before any self-ownership entry). Idempotent — skip if present.
- **Migrate the design-execution skill**: in `<PREFIX>-design/SKILL.md`, replace every `frontend-design` reference with `ui-ux-pro-max` (per preflight, warn with the link https://github.com/nextlevelbuilder/ui-ux-pro-max-skill if it isn't installed). Only touch installs that name `frontend-design` — a custom skill name chosen by the user stays. Idempotent — skip if no `frontend-design` reference exists.
- **Widen `<PREFIX>-design` to patterns and icons**: create `.claude/skills/<PREFIX>-design/references/ux-patterns.md` from `../../shared/tpl-lifecycle.md § <PREFIX>-design/SKILL.md` (its `references/ux-patterns.md` block), filling the `§ Iconography` source fields from what the project already depends on — read the manifest for an icon-set dependency and one call site for its import convention, and **never name a set the project doesn't have**; an empty field is honest, an invented one misroutes every future icon. Then re-sync the design SKILL.md's description, title line, external-skill note, MUST-invoke list, `## What this skill owns`, `## References` and `## Reference Sync` from the same section, preserving any project-authored content in those sections. Insert the interaction-pattern and icon trigger bullets plus the closing pattern/icon paragraph into the frontend skill's existing `## Visual Decisions` block (from `../../shared/tpl-domain-skill.md § Design delegation block`), and add the `ux-patterns.md` branch to `<PREFIX>-review/SKILL.md`'s `## Read Map` and `## References` — the branch is **conditional**, so add it only where a design skill exists. Apply as one unit: a reference nobody is routed to is never read, and triggers pointing at a missing file route into nothing. **Idempotent — skip only when** `ux-patterns.md` exists **and** the frontend `## Visual Decisions` block contains `interaction pattern` **and** `<PREFIX>-review/SKILL.md` names `ux-patterns.md`. Do **not** guard on the design description: it has opened with "MUST be invoked" since long before patterns or icons were in scope, so that phrase is true in every install this bullet exists to repair.
Update `<PREFIX>-skill` skill-manifest.md last: add the `<PREFIX>-deploy` lifecycle skill (with its `deploy-config.yaml` reference). Remove any `## Deployment` / `deploy-config.yaml` mention from the legacy domain owner's entry.

---

## Step 8 — Verify

Run these verification checks on the upgrade-affected items:

- `.claude/skills/<PREFIX>-deploy/SKILL.md` exists with a `## Deployment` section
- `.claude/skills/<PREFIX>-deploy/references/deploy-config.yaml` exists and parses as valid YAML
- No domain skill SKILL.md contains a `## Deployment` section (deploy logic lives only in `<PREFIX>-deploy`)
- `governed-paths.conf` has `DEPLOY_PATHS` and `PATH_MAP` references `<PREFIX>-deploy` (not `<LEGACY_DEPLOY_OWNER>`) for deploy-mechanism paths
- `ref-sync-check.sh` sources the conf and contains no inline path patterns
- `<PREFIX>-dev.md` step 4 is a single delegation paragraph — no inline `verify:` rules
- Neither `<PREFIX>-dev.md` nor `<PREFIX>-qa.md` contains the literal phrase `invoke the <PREFIX>-` (with `qa` or `pm`) `agent directly`
- `<PREFIX>-dev.md` has an explicit `Commit` step between Reference Sync and Hand off
- `<PREFIX>-qa.md` has an explicit `Reference Sync` step between Sign off and Hand off
- `.claude/graph/graph.py` is byte-identical to the plugin's `shared/graph.py` (`diff` is empty)
- `python3 .claude/graph/graph.py build` exits 0; running it twice produces an identical `edges.jsonl` body; every emitted edge has a non-empty `src` — when checking this, **skip the first line**, which is a `_meta` header (`built`/`edges`/`head`/`version`), not an edge. A naive "count lines without `src`" always returns 1 and is not a finding
- `build` reports **`N/N` log entries parsed** with no `WARNING` line. Any shortfall means entry headings are being silently dropped, which is a parser bug, not a tolerable loss — report it rather than accepting it. (Note the `LANDED_AS` count legitimately differs from the entry count in both directions: entries naming several commits emit several, and entries with a non-commit placeholder such as `pending` or `(uncommitted)` emit none.)
- `graph.py covers <paths>` returns the same set as the manual `paths:`-intersection rule for a sample of three historical tasks
- `<PREFIX>-graph/references/graph-schema.md` names both routes into open work and says a bare `fail` is not one of them — it must not present "latest `VERIFIED` edge is not a `pass`" as the entry condition, which is the discharge rule
- Every roadmap item has a unique `**Id:**`; `graph.py roadmap-open` lists the open ones
- `<PREFIX>-log/SKILL.md` contains `/tmp/<PREFIX>-skills-` scoped by an extracted `SESSION` id, and does **not** contain `PROJECT_ENCODED`, `~/.claude/projects`, or an mtime (`stat -f %m` / `stat -c %Y`) window
- If another repo on this machine shares this `<PREFIX>` (e.g. `<name>-agent` and `<name>-web`), confirm the next entry's `**Skills:**` lists only skills this project actually has — a sibling's skills appearing is the mtime-window bug, not a tuning issue
- On the next delivery after the upgrade, the entry's `**Skills:**` list contains `<PREFIX>-log` itself. It demonstrably ran — it wrote the entry — so its absence proves the derivation is still wrong. This is the cheapest available check that the fix took
- `pilot.md` contains `pilot-lane:`, `Resource ledger` and `Parked verdicts`; `<PREFIX>-log/SKILL.md` `Decisions:` guidance contains `parked (human-gated)`. Dispatching a task to a lane whose `spend` ledger is at 0 marks it `skipped — budget exhausted` rather than running it
- `<PREFIX>-dev.md` step 4 contains `Wait in the foreground`; both `<PREFIX>-dev.md` and `<PREFIX>-qa.md` `## Response Requirements` contain `Never end a turn without this block`; `code.md` and `fix.md` salvage protocols trigger on any non-`## Handoff` return (grep for `anything that is not a`) — all four, or a parked agent still stalls the pipeline with nothing reporting it
- **`code.md` and `fix.md` each contain `Done means:` at least three times** — once in the Step 0.5 Confirm question, once in the dev prompt, once in the qa prompt. Two occurrences means the statement is captured and then dropped: the agents that must build it and walk it never receive it, while the deferral branch downstream still judges whether a deferral "covers the end state of the acceptance statement". `/fix` shipped in exactly that state. **More generally: whenever `/code` and `/fix` are meant to share a mechanism, diff the two sections rather than checking either alone** — they are near-duplicates by design (Rule 3), so a change applied to one and not the other is invisible from inside either file
- `pilot.md` contains `prod-walk` and an `(a2) Prod walk` close-out step, and its scorecard has a `Prod walk` row; on the next mission that defers a prod-only check and then deploys, that check's `last:` is written at the deployed commit rather than left at its pre-deploy `blocked`
- `roadmap.md` contains `--items`, `roadmap-open` and `## Step 2 — Rank`; `pilot.md` cites `roadmap.md § Rank` and contains **no** rank rule of its own (the priority-then-dependency sentence is gone, not merely joined by a second copy); with 2+ open items `/roadmap` offers the mission route, with one it routes to `/code`/`/fix`
- Neither `code.md` nor `fix.md` contains `header: "Verify"` or `Run full regression after dev as well?`; both contain `--regression` and `regression_mode: auto`; the pm prompt's `Decisions:` template offers `agent` as an origin; `<PREFIX>-qa.md § Invocation modes` documents `auto` and its `Tests:` field carries the resolved scope
- `<PREFIX>-test/references/custom-tests.md` has a `## Regression scope` section whose fallback ladder ends at `smart`, **never** `full` — a missing graph must not silently widen every task
- `<PREFIX>-test/SKILL.md` tier table contains `non-negotiable`; `custom-tests.md` schema and its `## Record the outcome` step both name `reason`; `<PREFIX>-qa.md` `mode: retest` says it runs through step 4.5; `tweak.md`'s close-out walks the end state
- After the next task that records a `blocked` verification, `graph.py open-deferrals` renders a non-empty reason for it — the field is only useful if the writer actually writes it
- `<PREFIX>-log/SKILL.md` Process step 3 contains `never evidence a command`, and no install/upgrade doc still calls the marker `agent-scoped`
- `grep -l 'TRANSCRIPT=' .claude/hooks/*.sh` returns nothing — all four marker scripts key on `${SESSION_ID}` alone. (Grep the **assignment**, not `transcript_path`: the corrected comment names it on purpose, so a bare name grep flags a correct file.) Then confirm writer and readers agree: after any skill loads, `ls /tmp/<PREFIX>-skills-*` shows a file whose name carries the session id **once**, not twice
- `<PREFIX>-dev.md` step 1.5 contains "Match the metadata convention", and `git diff` on the upgrade touched `docs/roadmap.md` only for the `**Id:**` backfill — no reformatted metadata lines
- `<PREFIX>-log/SKILL.md` field guidance contains "field set above is closed", and its entry-format template carries no field outside the closed set
- Both graph artifacts are **ignored** — check the effect, not the text: `git check-ignore -v .claude/graph/edges.jsonl .claude/graph/__pycache__/x.pyc` names a matching rule for each, and `git status` is clean of graph artifacts after a build. Do **not** grep `.gitignore` for the literal paths: a repo-wide `__pycache__/` rule already covers the second one, and a literal grep reports a false gap on a correctly-configured project
- `git ls-files --error-unmatch .claude/graph/graph.py` exits 0 — the file is tracked, not merely present
- Deleting `edges.jsonl` mid-pipeline does not break `/code` — every call site falls back
- `<PREFIX>-pm.md` has an explicit `Reference Sync` step between Docs check and Verify log written
- `.claude/commands/wrap.md` exists; legacy `log.md` command file removed
- `.claude/commands/code.md`, `fix.md`, `roadmap.md`, `tidy.md`, `wrap.md` all exist
- If `<PREFIX>-design` exists: the frontend-owning skill contains a `## Visual Decisions — Delegate to <PREFIX>-design` section AND `<PREFIX>-design/SKILL.md` description starts with "Visual authority for <PROJECT>. MUST be invoked"
- `<PREFIX>-review/references/verification-before-completion.md` does not exist; `<PREFIX>-review/SKILL.md` `## References` points to `<PREFIX>-debug/references/verification.md`
- `<PREFIX>-qa.md` step 3 begins with `Test — invoke`; the `## Sign-off criteria` section is removed
- All three agents have the gated Reference Sync step and a `## Boundaries` section instead of `## What this agent does NOT do`
- `<PREFIX>-debug/SKILL.md` and `<PREFIX>-review/SKILL.md` contain a `## Read Map` section and **no** `## Reference Sync` section (static-content skills) — **or** were kept as deliberate customizations, in which case any `## Reference Sync` they carry lists exactly their project-authored references (no template-shipped files)
- `<PREFIX>-skill/SKILL.md` parity invariant contains "static-content" (the exception + content-ownership scoping, not the blanket every-skill form)
- `<PREFIX>-deploy/SKILL.md` `## Deployment` contains the strings "Caller contract", "ship-env", "Fill-in pass", and the inline gate context block fields (`env:`, `url:`, `command:`, `trigger:`, `commit:`, `branch:`)
- `<PREFIX>-deploy/SKILL.md` frontmatter does **not** contain `disable-model-invocation` (the skill must be model-invokable), and its `description:` contains "single source of truth for how it deploys"
- `<PREFIX>-dev.md` step 4 contains "target=non-prod" and does not contain "If a gate is declined"
- `<PREFIX>-pm.md` contains **no** prod-deploy step (no `target=prod`) and its `## Boundaries` says "No deployments"
- `<PREFIX>-pm.md` `## Boundaries` contains "Do not read `deploy-config.yaml`" (the deploy-tangent guard)
- `<PREFIX>-pm.md` ends with a `## Response Requirements` section whose `## Handoff` block declares `Status: complete | blocked`
- `<PREFIX>-pm.md` frontmatter contains `model: sonnet`
- `<PREFIX>-dev.md` contains the strings "## Handoff" and "Status: complete | blocked" (under the new layout these live in `## Response Requirements`, not step 7)
- `<PREFIX>-qa.md` step 2 contains "You do not edit code"
- `<PREFIX>-qa.md` contains "## Handoff" and "Status: signed-off | blocked" (under the new layout these live in `## Response Requirements`, not step 6)
- `<PREFIX>-pm.md` step 1 contains `**QA-evidence:**` and does **not** contain `PROJECT_ENCODED=`
- `.claude/commands/code.md` and `fix.md` parse `--prod` in Step 0 and end with a top-level "Deploy to prod (only if `--prod`)" step invoking `<PREFIX>-deploy target=prod`; their Step 0.5 capture proposes 2–3 candidate verifications via AskUserQuestion (not a prose prompt), sourced from the session plan's *Verification* section when one exists and inferred from the task otherwise; their persist step writes `task`/`assert` **single-quoted** and reduces `paths` to behavioral surface (contains the literal `behavioral surface`, keeps `.claude/skills/**/scripts/**`, excludes `docs/**` and `*.md`)
- `<PREFIX>-qa.md` contains a `## Invocation modes` section listing `mode: initial` and `mode: retest`
- `.claude/commands/code.md` Step 2 contains `mode=retest` and Step 3 contains `**QA-evidence:**`
- `.claude/commands/fix.md` Step 3 contains `mode=retest` and Step 4 contains `**QA-evidence:**`
- `<PREFIX>-dev.md` and `<PREFIX>-qa.md` both end with a `## Response Requirements` section (the **last** section, after `## Boundaries`) containing the imperative "Every response MUST end with the `## Handoff` block" and the block format; their workflow Hand off step (dev step 7, qa step 6) is a one-line pointer to that section
- Every domain skill SKILL.md (those with `## Architecture`) contains a `## Preconditions — Verify Before Writing References` section between `## Architecture` and the next section
- `<PREFIX>-review/references/issuing-findings.md` exists; `<PREFIX>-review/SKILL.md` `## Read Map` contains the literal string `Issuing review findings`; `## References` lists `references/issuing-findings.md`
- `<PREFIX>-review/references/security-review.md` exists; `<PREFIX>-review/SKILL.md` `## Read Map` and `## References` reference `security-review.md`
- `<PREFIX>-test/references/` contains `custom-tests.yaml` (`tests: []`), `custom-tests.md`, and `test-commands.md` with `## Smoke` / `## Regression` / `## Functional Feature Subjects` headings (existing content preserved)
- `<PREFIX>-test/SKILL.md` `## Test Plan` lists Smoke / Functional Feature / Regression tiers; there is **no** `## E2E Browser Tests` section; `## References` and `## Reference Sync` both list the four reference files (`test-commands.md`, `custom-tests.yaml`, `custom-tests.md`, `sync-checklist.md`)
- `<PREFIX>-test/references/sync-checklist.md` has no "E2E browser target URL" line and contains an `## Update references/custom-tests.yaml when:` section
- `<PREFIX>-test/references/custom-tests.md` contains the "Running-stack rule", a single-quoted schema, and the `type: UX | Integration | E2E` taxonomy (no `surface`)
- `<PREFIX>-qa.md` `## Invocation modes` includes `regression_mode: smart | full`; step 3 forwards `regression_mode`, new-entry names, and changed paths to `<PREFIX>-test`
- `.claude/commands/code.md` contains "What should be verified before this ships" (Step 0.5) and a Step 1.5 persist; `.claude/commands/fix.md` contains the same Step 0.5 and a Step 2.5 persist
- `deploy-config.yaml` has no top-level `local:` block and no `cloud:` prefix in any `verify:`; every env declares exactly one of `run:`/`deploy:` plus a `url`
- Every locally-runnable component in `deploy-config.yaml` has at least one non-prod env (an `envs.local` serve-env or a cloud non-prod ship-env)
- `<PREFIX>-test/references/custom-tests.md` Execution resolves UX/E2E to the "first non-prod env url" and the Running-stack rule contains "never count component/unit tests"
- `<PREFIX>-qa.md` step 5 (Sign off) contains "UAT-deferral is the command's" (the blocked-verification mapping)
- `.claude/commands/code.md` and `fix.md` both contain an "Ensure verification stack" step before the qa step and a "Defer to UAT" branch in the qa-handoff parse
- `<PREFIX>-qa.md` handoff block declares `signed-off | signed-off-with-deferrals | blocked` with `Evidence:` and `UAT-deferred:` fields; step 2 splits source vs non-source findings; step 1 contains the per-file diff-read rule
- `<PREFIX>-pm.md` step 1 accepts `signed-off-with-deferrals`; step 2 contains "primary feature/fix commit"; step 2.5 says "required, not best-effort"
- `<PREFIX>-dev.md` step 3 contains "non-interactively"
- `.claude/commands/code.md` and `fix.md` contain "Gate policy", "--no-push", "Salvage protocol", "Honor the user's answer", "signed-off-with-deferrals", "Feature commit:", and a "Close out: push + verified scorecard" step
- `.claude/commands/tweak.md` and `revert.md` exist; `tweak.md` contains "batched close-out"; `revert.md` contains "git revert" and no `git reset`
- `.claude/commands/tidy.md` exists and contains "history probe", the disposition vocabulary (`commit`/`deliver`/`discard`/`ignore`/`keep`), `tidy-discard-`, and `git branch -d`; `code.md`, `fix.md`, and `pilot.md` entry hygiene routes overlapping WIP to `/tidy`; `CLAUDE.md` `## Agents` routing paragraph mentions `/tidy`
- `.claude/commands/pilot.md` exists and contains "Autonomy contract", "Fly this mission", `max_tasks`, "auto-accepted — pilot run", "Split broad tasks", and a "mission report" Done section; `CLAUDE.md` `## Agents` routing paragraph mentions `/pilot`
- `.claude/commands/wrap.md` contains a conditional `## Step 0 — Review` and a `## Step 4 — Push + scorecard`
- `<PREFIX>-deploy/SKILL.md` contains `## Push policy`, `gate: unanswered — parked`, and the non-prod early exit
- `<PREFIX>-test/references/custom-tests.md` contains "Freshness rule" and "Evidence trace"
- `<PREFIX>-log/SKILL.md` Process step 1 contains "primary feature/fix commit"; entry format has the `UAT-deferred:` line
- `pre-handoff-check.sh` contains `subagent_type` and no `GEMINI`; settings.json wires it under a `Task|Agent` matcher
- `pre-handoff-check.sh` lint/typecheck command is self-contained from a bare shell (env-launcher, absolute env path, or a resolved interpreter variable — not a bare `ruff`/`python -m ruff`/`eslint`/`tsc`)
- `close-out-gate.sh` exists, is executable, and is wired under PreToolUse Bash
- `ref-sync-check.sh` contains `REF_WATCH` and the A/D/R structural filter
- `governed-paths.conf` has a `REF_WATCH` line and per-skill self-ownership entries before the `.claude/skills/` catch-all
- `CLAUDE.md` `## Agents` contains the `/tweak` routing paragraph and a `## Secrets` section exists
- Every domain skill `## Quality Checklist` contains "non-interactive" and "standalone"
- Every domain skill `## Quality Checklist` command resolves against the project (named `package.json` scripts exist under `scripts`; linters/tools are installed deps or resolvable paths); the lint command and `pre-handoff-check.sh` `<LINT_CMD>` agree (same verified command, or both absent)
- `.claude/commands/code.md` and `fix.md` contain "Single gate" (one AskUserQuestion for confirm + verify + regression + Ship), `ship_mode`, the prod-step "Pre-authorization" conditions, and the stash-at-entry wording; their close-out contains "Restore stashed WIP"
- `<PREFIX>-deploy/SKILL.md` gate contains "Pre-authorization" and `gate: pre-authorized`
- If `<PREFIX>-design` exists: its SKILL.md contains no `frontend-design` reference (delegates to `ui-ux-pro-max` or a deliberate custom choice)
- If `<PREFIX>-design` exists: `references/ux-patterns.md` contains `## Inventory`, `## Consistency sweep` and `## Iconography`; its SKILL.md MUST-invoke list names both `interaction pattern` and `icon`; the frontend skill's `## Visual Decisions` block names both; and `<PREFIX>-review/SKILL.md` names `ux-patterns.md` in its `## Read Map` **and** `## References`. All four — the sweep is otherwise reachable from nowhere, or reachable and checked by nobody
- If `<PREFIX>-design` exists: `§ Iconography` contains the never-hand-author rule **and** the missing-credential guard (a key that isn't set means the icon is reported blocked, never replaced with something drawn), and the design description still opens `Visual authority for <PROJECT>. MUST be invoked`
- If **no** `<PREFIX>-design` exists: `grep -rl ux-patterns .claude/skills/` returns nothing — the review branch is conditional, and a branch to a file no skill owns is a dead route
- On the next UI task in this install, `ux-patterns.md § Inventory` gains a row and the delivery log's `**Skills:**` lists `<PREFIX>-design`. A task that changed a surface and left the Inventory untouched means the sweep is being skipped — the same silent-skip class as a verification nobody runs
- `<PREFIX>-test/SKILL.md` `## Rules` contains "Never re-run domain unit"
- `<PREFIX>-qa.md` step 3 contains "Verify outcomes, not environments"
- `governed-paths.conf` `PATH_MAP` contains the `custom-tests\.yaml$:EXEMPT` entry
- `skill-manifest.md` is current

Report a summary of what was upgraded.
